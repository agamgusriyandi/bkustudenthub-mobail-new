# Changelog

Semua perubahan dan pembaruan penting pada proyek UBKMobail akan didokumentasikan dalam berkas ini.

## [Unreleased] - 2026-07-16

### Added
- **Unified Button Widget (`UnifiedButton`):**
  - Membuat widget tombol kustom `UnifiedButton` untuk membakukan ukuran, bentuk, tipografi, dan skema warna tombol secara konsisten di seluruh modul dan peran.
  - Mengintegrasikan deteksi teks cerdas (case-insensitive) sehingga tombol aksi bermakna pembatalan ("Batal", "Tutup", "Tolak") otomatis berwarna Merah semantik (`theme.danger`), sementara tombol aksi bermakna persetujuan ("Setuju", "Konfirmasi", "Simpan", "Kirim", "Reschedule") otomatis berwarna Hijau semantik (`theme.success`), dan tombol lainnya secara default mengikuti warna dinamis dari API (`theme.primary`).

### Changed
- **Penyelarasan Desain Tombol Global:**
  - Mengintegrasikan `UnifiedButton` di dalam `CustomDialog` sebagai tombol konfirmasi dialog default, sehingga semua dialog konfirmasi pada seluruh peran dan halaman secara otomatis mengadopsi standarisasi tombol baru.
  - Melakukan refaktorisasi pada halaman modul konseling (`student_counseling_screen.dart`, `counseling_booking_screen.dart`, `add_schedule_slot_screen.dart`, `session_note_screen.dart`, `assessment_management_screen.dart`) untuk menggunakan `UnifiedButton`.
  - Mengganti `AnimatedUnifiedCard` yang tidak terdefinisi kembali menjadi `AnimatedContainer` untuk item navigasi tab / filter pill interaktif pada halaman daftar booking psikolog (`psychologist_bookings_screen.dart`) dan manajemen rujukan (`referral_management_screen.dart`).
  - Mengganti `UnifiedCard` yang tidak memiliki parameter `alignment` kembali menjadi `Container` pada area latar Dismissible swipe halaman notifikasi psikolog (`psychologist_notifications_screen.dart`).
- **Refaktorisasi Desain & Komponen Modul Tenaga Kesehatan (Nakes):**
  - Melakukan migrasi Container kartu data, summary statistik, detail riwayat, BAP kegiatan, klaim asuransi, dan profil pada seluruh halaman presentasi Nakes (`lib/features/tenaga_kesehatan/presentation/pages/` & `widgets/`) agar konsisten menggunakan komponen `UnifiedCard`.
  - Melakukan pembakuan seluruh tombol aksi (kembali, simpan, ekspor PDF/Excel, form offline, download rujukan, persetujuan klaim, registrasi manual, input screening) dengan `UnifiedButton` yang mendeteksi warna tombol secara dinamis dan mematuhi skema warna semantik global (Hijau untuk aksi setuju/simpan, Merah untuk batal/kembali/tolak, dan warna API primer untuk aksi umum lainnya).

### Fixed
- **Sintaksis & Pemulihan Halaman Konseling:**
  - Memperbaiki seluruh masalah kerusakan sintaksis, duplikasi blok UI, dan ketidaksesuaian tanda kurung (*bracket mismatch*) pada halaman modul konseling psikolog dan mahasiswa yang sempat terganggu oleh skrip refaktorisasi otomatis.
  - Menghapus variabel dan berkas import yang tidak terpakai untuk memastikan status kompilasi **0 issues found** di konsol `flutter analyze` untuk seluruh folder `lib/features/counseling/`.
- **Pembersihan Warning Linting Proyek:**
  - Menyelesaikan warning variabel lokal yang tidak terpakai (`themeProvider`, `btnBg`, `btnFg`, dan `theme`) di `klinik_booking_screen.dart`, `rekrutmen_ormawa_screen.dart`, dan `tk_booking_screen.dart` untuk menjaga codebase 100% bebas issue.
  - Menghapus berkas import dan deklarasi variabel lokal tidak terpakai (`app_radius.dart`, `theme`) pada berkas presentasi Nakes (`tk_patient_card.dart` dan `tk_settings_screen.dart`) sehingga kompilasi modul `tenaga_kesehatan` lolos dengan **0 issues found**.

## [Unreleased] - 2026-07-14

### Changed
- **Redesain Halaman Input Screening Nakes:**
  - Merubah visual Step 2 (Data Vital) dari slider vertikal yang panjang menjadi **2-Column Grid Input Cards** modern dengan input teks langsung.
  - Mengeliminasi warna oranye di sepanjang alur screening (Tombol "Lanjut", "Scan QR Code", progress bar dots, focused borders pada isian form, dan dialog rujukan) menjadi warna Deep Blue khas BKU (`AppColors.primary`) dan neutral kelabu modern.
  - Menyelesaikan seluruh deprecation warning (`withOpacity`, `value` pada dropdown) untuk mencapai status **`0 issues found`** di `flutter analyze`.
- **Pembersihan Warna Biru & Oranye pada Form Screening (Lanjutan):**
  - Membersihkan sisa-sisa warna biru (`AppColors.primary`) dari halaman `tk_screening_input_screen.dart` (termasuk slider thumb, indikator tahapan, outline form) ke warna Hijau elegan (`AppColors.success`) dan warna netral abu-abu.
  - Memperbarui gaya tombol "Lanjut" dan "Simpan" agar menggunakan gaya *clean white* (latar putih, teks abu gelap, dengan outline lembut) menggantikan desain hitam tebal.
- **Modernisasi UI Form & Tombol Secara Global:**
  - Menginjeksi `inputDecorationTheme` secara sentral ke dalam `lib/main.dart` untuk membakukan desain seluruh form aplikasi dengan latar abu-abu halus (`AppColors.neutral50`), padding proporsional, dan *focus border* tipis berwarna primer.
  - Memperbarui parameter dasar `ElevatedButtonThemeData` dan `OutlinedButtonThemeData` agar seluruh tombol di aplikasi tampil lebih konsisten, empuk, dan modern.
  - Melakukan *refactoring* massal terhadap 41 file fitur yang sebelumnya me-override form dengan `fillColor: Colors.white` statis, menjadi gaya `AppColors.neutral50` yang seragam.
- **Kompilasi Ulang APK Rilis Final:**
  - Sukses mengompilasi ulang master APK rilis terbaru (`app-release.apk`) yang berisi pembaruan seluruh desain form secara global dan perbaikan crash login.

### Fixed
- **Perbaikan Eror & Warning Kompilasi Proyek (Zero Warnings):**
  - Menyelesaikan seluruh masalah bracket mismatch, bracket closure, dan sisa syntax error pada halaman pendaftaran konseling (`counseling_booking_screen.dart`), pendaftaran kesehatan (`health_booking_form_screen.dart`), halaman banding Kencana (`kencana_banding_screen.dart`), dan seluruh dialog profil (`profile_dialogs.dart`).
  - Mengintegrasikan overlay loading dinamis pada avatar Tenaga Kesehatan di halaman pengaturan (`tk_settings_screen.dart`) saat mengunggah foto profil baru.
  - Memastikan seluruh modul lulus `flutter analyze` dengan **0 issues found** (0 errors dan 0 warnings).
- **Perbaikan Crash/Force Close Saat Login di Mode Rilis:**
  - Mendiagnosis dan mengimplementasikan mekanisme *reset on error* (`resetOnError: true`) pada modul `FlutterSecureStorage` bawaan (`biometric_service.dart`) untuk mencegah aplikasi force close/crash seketika akibat kegagalan sinkronisasi kunci dekripsi (SecurityException) dari Android Keystore saat instalasi ulang.
- **Perbaikan Rendering Avatar Pasien/Mahasiswa yang Kosong:**
  - Merombak arsitektur rendering avatar (`CircleAvatar`) pada pemilihan pasien dengan membungkusnya dalam *Builder* pintar untuk mengecek validitas skema URL (memastikan hanya URL berawalan `http` dan bukan sekadar kata `"null"` yang diteruskan ke Image.network).
  - Menambahkan perlindungan lanjutan berupa parameter `errorBuilder` untuk menangani *broken link* atau 404 dari sisi server, sehingga fallback inisial nama pasien akan tetap tampil dengan mulus (*gracefully*) ketika gambar gagal dimuat.

### Changed
- **Pembersihan Oranye & Redesain UI/UX ke Gaya Minimalist Clean White:**
  - Mengubah seluruh area tubuh (body screens) dari warna arang/hitam (`neutral800`) dan warna oranye/biru dinamis menjadi gaya **Minimalist Clean White**: latar belakang putih bersih (`Colors.white`), borders kelabu tipis (`AppColors.neutral300`), dan teks serta ikon gelap (`AppColors.neutral800` / `AppColors.neutral600` / `AppColors.onSurface`).
  - Menerapkan gaya Minimalist Clean White ini pada halaman Beasiswa (`scholarship_screen.dart`, `apply_scholarship_screen.dart`), Prestasi (`achievement_screen.dart`, `report_achievement_screen.dart`), Suara Mahasiswa (`student_voice_screen.dart`, `submit_aspiration_screen.dart`), Notifikasi (`notifications_screen.dart`), Kencana (`kencana_screen.dart`, `kencana_score_screen.dart`, `kencana_invitations_screen.dart`, `kencana_banding_screen.dart`, `kencana_qr_scan_screen.dart`, `quiz_screen.dart`).
  - Mengaudit dan membersihkan seluruh folder **Organisasi** (`organisasi_screen.dart`, `daftar_ormawa_screen.dart`, `add_organisasi_screen.dart`, `rekrutmen_ormawa_screen.dart`) ke gaya Minimalist Clean White (menyinkronkan button, focused borders, uploader, dan card banner).
  - Mengaudit dan membersihkan seluruh folder **Konseling** (`counseling_screen.dart`, `psychologist_list_screen.dart`, `book_counseling_screen.dart`) dengan mengubah seluruh slot waktu, pilihan metode sesi, card psikolog, dan button menjadi gaya Minimalist Clean White.
  - Memastikan seluruh kode lulus analisis Dart (`flutter analyze`) dengan tepat **0 issues found**.

## [Unreleased] - 2026-07-13

### Changed
- **Pembersihan Oranye Total di Area Tubuh (Body) Peran Student:**
  - Mengeliminasi warna oranye dinamis dari API (`Theme.of(context).colorScheme.primary`) pada ChoiceChips, FAB melayang, garis tepi kartu (borders), tombol popup, dialog, bottom sheet, kartu IPK prestasi, uploader file, dan area konten utama di halaman Beasiswa, Prestasi, Suara Mahasiswa, dan Kalender Kegiatan.
  - Mengubah seluruh elemen visual tersebut ke warna institusional **Deep Blue** (`AppColors.primary`) dan warna netral kelabu (`AppColors.neutral300` / `AppColors.onSurface`) secara rapi dan konsisten sesuai kebijakan visual premium.
  - Menormalisasi ikon piala prestasi, teks judul prestasi ("TESS", "galatama"), pemilih media (Kamera/Galeri), uploader kosong, indikator progres kuis, dan menu Kencana menjadi abu-abu netral (`AppColors.neutral600` / `AppColors.neutral100`) dan warna netral gelap (`AppColors.onSurface`) untuk hasil visual yang super bersih (*clean* & *premium*).
- **Redesain UI/UX Dasbor Student & Normalisasi Teks**:
  - Mendesain ulang `StudentServiceGrid` dengan kontainer ikon lingkaran penuh (`BoxShape.circle`) yang memiliki border mikro-outline halus (`Border.all`) untuk memberikan kedalaman visual yang premium.
  - Mendesain ulang kartu progres metrik `StudentStatusGrid` dari tata letak horizontal (`Row`) menjadi vertikal bertumpuk (`Column`) bergaya dasbor modern (metrik ala Apple Health) dengan ukuran teks metrik yang lebih besar, ikon bulat di kiri atas, dan indikator panah navigasi mini di kanan atas.
  - Mengimplementasikan *Neutral Text Policy* (Kebijakan Pewarnaan Teks Bersih) pada halaman kuis (`quiz_screen.dart`), detail modul (`module_detail_screen.dart`), dan detail sesi (`kencana_session_screen.dart`) dengan mengubah seluruh warna teks judul, pertanyaan, dan deskripsi dari oranye primer dinamis (`colorScheme.primary`) menjadi warna netral gelap (`AppColors.onSurface`) demi mencegah kejenuhan warna oranye (*orange overload*) pada mata pengguna.
- **Sinkronisasi Progress Materi Pembelajaran**: Memperbaiki status penyelesaian materi belajar agar tersinkronisasi ke backend dengan menambahkan metode `completeMaterial` pada `KencanaProvider`, memanggil API POST `/kencana-student/materials/{materialId}/complete` saat tombol "Tandai Selesai Belajar" ditekan di `ModuleDetailScreen`, menampilkan `CustomDialog` sukses tipe premium, serta memicu pemuatan ulang detail sesi (`fetchSessionDetails`) otomatis ketika halaman kembali (*pop*) ke layar sesi utama untuk merender ikon ceklis hijau sukses secara instan.
- **Dukungan Soal Essay pada Kuis**: Menambahkan area input teks (`TextField`) khusus untuk soal bertipe essay di `quiz_screen.dart`, mengintegrasikan pemutakhiran state index `_updateQuestionIndex` agar teks tersimpan secara persisten, memperbarui validasi tombol "Pertanyaan Selanjutnya" agar tidak memblokir tipe essay, dan mengirimkan payload jawaban dengan parameter `answer_text` sesuai kontrak API backend.
- **Pembersihan HTML Tag Berita Kampus:** Mengintegrasikan pembersih tag HTML (`_parseHtmlString`) pada konten agenda berita kampus di dasbor utama mahasiswa (`StudentAgendaList`) untuk membersihkan tag mentah seperti `<p>`, `</p>`, dan entitas HTML `&nbsp;`.
- **Pendaftaran Beasiswa & Rubrik Penilaian:** Mengintegrasikan formulir kuesioner dinamis untuk skema khusus beasiswa (excellence, impact, hope_grant, tahfidz) di mobile dan mengirimkannya sebagai payload JSON string `rubrik_answers`.
- **Pembaruan UI/UX Premium Dialog & Modal**: Merancang ulang `CustomDialog` dengan kelengkungan `AppRadius.radiusXl`, layout terpusat yang seimbang, ikon semantik halus sesuai status aksi (sukses, gagal/destructive, loading, info), serta hierarki tombol interaktif yang rapi. Memperbarui `rejection_bottom_sheet.dart` dengan menambahkan indikator *drag handle* di bagian atas, merapikan struktur jarak menggunakan token `AppSpacing`, serta merancang ulang kartu statistik pendaftar dengan gaya *glassmorphism* modern yang responsif pada berbagai ukuran layar.
- **Proteksi Double Apply Beasiswa:** Memperbarui halaman detail beasiswa agar langsung menampilkan bottom sheet penolakan (`showRejectionBottomSheet`) lengkap dengan visual gambar penolakan `ubk_rejection.png` apabila mahasiswa mengklik daftar untuk program beasiswa yang sudah didaftarkan sebelumnya. Tapping (klik) langsung pada kartu beasiswa yang sudah didaftarkan di list "Pendaftaran Aktif" kini langsung memicu bottom sheet penolakan secara instan tanpa membuka popup detail terlebih dahulu. Pemilihan kelayakan mendaftar (`_checkAndNavigateToApply`) kini disederhanakan dan diperkuat dengan mendeteksi adanya pendaftaran aktif baik di tingkat memori lokal (`SharedPreferences`) maupun data ter-sync dari backend (`student.scholarships` status). Sebagai proteksi berlapis (*defense-in-depth*), validasi ini juga disematkan langsung pada `initState` `ApplyScholarshipScreen` (menggunakan `postFrameCallback`), sehingga jika mahasiswa memintas validasi dari halaman utama, formulir pendaftaran akan langsung ditutup otomatis dan digantikan dengan bottom sheet penolakan.
- **Refactor UI Engineering & Standardisasi Desain Token:** Melakukan pembersihan massal terhadap warna, jarak (*spacing*), dan kelengkungan (*border radius*) yang *hardcoded* di seluruh folder `lib/features/` (mencakup **180+ file**).
- **Desain Token Spasi & Radius Baru:** Membuat `app_spacing.dart` dan `app_radius.dart` di dalam `lib/core/theme` sebagai pusat konfigurasi *margin*, *padding*, dan *border radius* (menghilangkan kecenderungan lengkungan acak sisa *AI-generated*).
- **Pembersihan Warna Hardcoded:** Mengonversi penggunaan warna Hex-Code statis (seperti `Color(0xFFF1F5F9)`) dan warna statis Flutter (seperti `Colors.green`, `Colors.blue`) menjadi *Semantic Tokens* dari `AppColors` secara aman untuk mempermudah implementasi *Dark Mode* ke depannya.
- **Perbaikan Konstanta & Compile-time Safety:** Mengubah variabel `AppColors` dan `AppRadius` menjadi `static const` dari yang sebelumnya `static Color` / `static final` agar dapat dipanggil dalam konstruktor widget konstan secara aman.
- **Standardisasi Skala Warna Abu-Abu Netral (Kategori A):** Menyisir dan mengganti sisa Hex-Code abu-abu netral mentah (*slate/gray* seperti `0xFF111827`, `0xFF1E293B`, `0xFF4B5563`, `0xFF9CA3AF`, `0xFFE5E7EB`, dan `0xFFF9FAFB`) yang tersebar di halaman login, pendaftaran sandi, dialog profil, pengaturan nakes, dan pengaturan psikolog ke dalam semantic design tokens `AppColors.neutralX00` secara aman (termasuk penanganan multiline formatting dan pembersihan keyword `const` sebelum instansiasi).
- **Refaktor Struktur Folder (Modularitas 100%):** Memindahkan seluruh berkas provider spesifik fitur (`student_provider.dart`, `ormawa_provider.dart`, `kencana_provider.dart`, `achievement_provider.dart`, dan `scholarship_provider.dart`) dari folder sentral `lib/core/providers/` ke direktori modul fitur presentasinya masing-masing agar mematuhi arsitektur *Clean Architecture* dan pola *Feature-First*.
- **Pembersihan Berkas Mati (Dead Code):** Menghapus duplikat file `ormawa_provider.dart` kosong yang tidak terpakai di bawah folder fitur ormawa.
- **Mentor Kencana (Redesain UI/UX Lengkap):** Melakukan redesain menyeluruh pada seluruh halaman utama peran Mentor Kencana (Dashboard, Kelompok Saya, Rekrut Mentee, Detail Mentee & Tugas/Catatan, Surat Izin Mentee, Absensi Sesi, dan Penilaian Mentee) agar memiliki visual yang premium, bersih, dan konsisten dengan desain peran Mahasiswa (menggunakan latar belakang `Color(0xFFF8FAFC)`, kartu putih bersudut melingkar `BorderRadius.circular(24)`, border halus `surfaceContainerHighest`, shadow minimalis, inisial avatar melingkar berwarna primary, serta input textfield dan dropdown yang rapi).
- **Mentor Kencana (Peningkatan Dialog Konfirmasi & Modals):** Mengintegrasikan `CustomDialog` standar untuk menggantikan dialog bawaan sistem pada proses penolakan/persetujuan izin mentee, penginputan absensi manual, dan penginputan nilai kognitif/psikomotor/afektif mentee.

### Fixed
- **Penyesuaian PDF Generator:** Memperbaiki kesalahan *import* dan jenis *BorderRadius* / *Color* pada *generator* PDF portofolio yang sempat rusak akibat pembersihan massal.
- **Mentor Kencana (Deprecation & Compilation Fixes):** Membersihkan seluruh warning kompilasi, memperbaiki warning penggunaan `withOpacity` yang didepresiasi dengan menggantinya ke `withAlpha`, memperbaiki warning penggunaan `value` pada `DropdownButtonFormField` dengan beralih ke `initialValue`, dan menyematkan import `custom_dialog.dart` yang terlewat pada halaman surat izin mentee.
- **Mentor Kencana (Bulk Scoring/Input Nilai):** Memperbaiki bug payload mismatch saat menyimpan nilai mentee. Mobile sekarang memformat input Kognitif, Psikomotor, dan Afektif ke dalam struktur bulk `scores` dan `items` yang diekspektasi backend (sesuai skema `BulkScoresReq`).
- **Mentor Kencana (Validasi Surat Izin):** Memperbaiki parameter status yang dikirim saat menyetujui/menolak izin dari mobile. Mengubah parameter payload dari `{"status": "Approved"}` menjadi `{"action": "approved"}` (atau `"rejected"` dalam huruf kecil) agar sinkron dengan model penampung di backend.
- **Mentor Kencana (Absensi Sesi & QR Code):** Memperbaiki bug QR Code kosong/tidak valid pada mobile. Menambahkan penarikan token QR secara dinamis melalui endpoint `GET /kencana-mentor/sessions/:sessionId/qr-token` saat tombol QR diklik.
- **Mentor Kencana (Absensi Manual):** Memperbaiki kegagalan penyimpanan absensi manual (silent fail) di backend. Mobile kini otomatis mencari `student_id` berdasarkan `nim` dari memori kelompok mentee (dan menarik list kelompok jika kosong), memetakan status kehadiran menjadi format backend (`present`, `permission`, `absent`), serta membungkusnya dalam format array `attendances`.
- **Mentor Kencana (Dashboard Polling Interval):** Mengoptimasi interval polling dasbor Mentor Kencana dari yang awalnya setiap 3 detik menjadi setiap 60 detik guna menghemat baterai perangkat mobile dan mengurangi beban trafik server.
- **Mentor Kencana (Parsing Kelompok Mentee):** Memperbarui `MenteeGroup` and `MenteeData` JSON parser untuk mendukung data bersarang (nested `student` map) dan properti key `members` yang dikembalikan dari API backend agar list mentee dan NIM tampil dengan benar.
- **Mentor Kencana (Import Mentee Detail):** Memperbaiki eror kompilasi `Undefined class 'MenteeDetailData'` di berkas `mentor_mentee_detail_screen.dart` dengan menambahkan import model `mentor_models.dart` yang terlewat.

## [Unreleased] - 2026-07-12

### Added
- **Konseling (Rute & Menu Asesmen Psikolog):** Mendaftarkan rute GoRouter (`AppRoutes.assessmentManagement` dan `AppRoutes.assessment`) untuk halaman `AssessmentManagementScreen` dan `AssessmentScreen` di `app_routes.dart`. Selain itu, menambahkan tombol menu **"Asesmen"** pada grid menu utama `psychologist_service_grid.dart` (Layanan Utama) agar fitur asesmen/screening kesehatan mental mahasiswa dapat diakses secara penuh oleh Psikolog di aplikasi mobile selayaknya pada versi website.

### Changed
- **Organisasi (Tambah Organisasi Redesign):** Redesigned the "Tambah Organisasi" screen to match the website form layout: replaced the Date Picker with numeric year input fields ("Tahun Mulai" and optional "Tahun Selesai"), updated the "Tipe Organisasi" options to match the web dropdown (UKM, Himpunan Prodi, BEM, DPM, Komunitas, Lainnya), added red asterisk indicators (`*`) to required fields, and updated placeholders/hints to match the web form exactly.

### Fixed
- **Mentor Kencana (Unggah Foto Profil):** Mengimplementasikan fitur unggah/ganti foto profil kustom secara langsung di aplikasi mobile untuk peran Mentor Kencana. Menambahkan tombol edit (kamera) di sudut kanan bawah avatar pada halaman `mentor_profile_screen.dart`, serta menambahkan fungsi `uploadAvatar` pada `AuthService` untuk mengirimkan foto ke endpoint `/auth/profile/upload-avatar`.
- **Mahasiswa (Tema Dinamis Halaman Prestasi):** Mengganti referensi warna statis `AppColors.primary` dan `AppColors.outline` pada kartu riwayat & detail prestasi menjadi `Theme.of(context).colorScheme.primary` dan `Theme.of(context).colorScheme.outline` agar tampilan teks, label, dan ikon tersinkronisasi secara otomatis saat admin mengubah tema warna brand aplikasi dari panel web.
- **Mahasiswa (Layout Tombol Tambah Prestasi):** Memperbaiki posisi tombol "Tambah Prestasi" (Floating Action Button) pada halaman riwayat prestasi mahasiswa agar tidak tertutupi oleh navigasi bawah (`UnifiedBottomNavBar`). Menambahkan padding bawah sebesar 90dp pada tombol tersebut.
- **Mentor Kencana (Foto Profil Kedap-Kedip):** Memperbaiki masalah di mana foto profil di mobile berkedip (flicker) setiap 3 detik akibat trigger background polling dashboard. Logika pembacaan foto di `auth_service.dart` kini dimodifikasi agar hanya melakukan pembersihan/refresh cache buster (`ApiGate.refreshAvatarCache()`) apabila berkas foto (`studentFoto`) benar-benar mengalami perubahan dari nilai sebelumnya.
- **Mentor Kencana (Perbaikan Foto Profil):** Memperbaiki bug di mana foto profil Mentor Kencana tidak tersinkronisasi dengan foto baru hasil unggahan dari website. Mengatur prioritas logika pembacaan foto di `auth_service.dart` agar mendahulukan berkas custom avatar (`user.avatar_url`) daripada foto default akademik (`mahasiswa.foto` dari SEVIMA).
- **Konseling (Tindak Lanjut Rujukan):** Memperbaiki tampilan nama, NIM, dan gambar avatar mahasiswa pada halaman "Tindak Lanjut" (`ReferralManagementScreen`). Pengekstrakan data di model `Referral.fromJson` kini diperkuat untuk mendeteksi variasi key secara langsung di tingkat root (seperti `NIM`, `nim`, `FotoURL`, `Foto`, dsb.) sehingga data tidak lagi kosong.
- **Konseling (Avatar Pasien/Mahasiswa Psikolog):** Memperbaiki dan memperkuat logika pengekstrakan avatar mahasiswa/pasien pada halaman `PsychologistBookingsScreen` (termasuk list card & detail bottom sheet) dan `PatientListScreen` (termasuk list card & detail header). Sekarang sistem dapat merender foto profil asli mahasiswa secara dinamis menggunakan `ApiGate.getImageUrl` dengan memeriksa berbagai format key di level root (seperti `FotoURL`, `Foto`, `foto_url`) maupun key bersarang, sebagai pengganti inisial teks statis.
- **Sesi Pengguna (Redirection Guard GoRouter):** Memperbaiki bug di mana pengguna non-mahasiswa (seperti Mentor Kencana, Psikolog, Tenaga Kesehatan, dan Ormawa) tetap berada di halaman utama mahasiswa (`/main` atau `/`) pada saat aplikasi dimuat ulang/direstart (hot restart / hot reload). Menambahkan logika `redirection guard` global di `app_routes.dart` untuk mendeteksi peran aktif pengguna dan secara otomatis mengarahkan ke dashboard yang sesuai (misalnya `/mentor-kencana`) guna mencegah konflik hak akses (403 Forbidden).
- **Auth (Perbaikan Login Multi-Peran Double Degree):** Memperbaiki bug pemilihan peran (role selection) Mahasiswa Double Degree yang memicu kesalahan sistem (`Terjadi kesalahan sistem. Silakan coba lagi`). Menyesuaikan payload request ke endpoint `/auth/login/select-role` di mana parameter NIM yang dipilih kini dikirimkan secara terpisah melalui field `selected_nim`, dan field `selected_role` dikirim sebagai `"student"` sesuai dengan kontrak API backend terbaru.
- **Mentor Kencana (Sinkronisasi Foto Profil):** Memperbaiki sinkronisasi foto profil (PP) peran Kencana Mentor agar selaras dengan data profil Mahasiswa di web dan mobile secara dinamis dan real-time. Helper `_getMentorAvatarUrl` pada `mentor_profile_screen.dart` dan `mentor_dashboard_screen.dart` dikonfigurasi untuk hanya memuat data foto profil dari properti `studentAvatarUrl` di `AuthService`. Menghapus filter placeholder `admin_` pada `StudentProvider` and `AuthService` karena nama berkas unggahan yang dihasilkan backend untuk Mentor memiliki awalan `admin_` (misalnya `admin_20948_1783818472.jpg`), sehingga foto profil yang valid tidak lagi salah dideteksi sebagai placeholder. Fungsi `fetchMe` dan `_loadCachedStudentAvatar` di `AuthService`, serta parser pada `StudentProvider` (baik pada `loadAllData` maupun `fetchProfile`), diperbarui untuk memetakan dan menyinkronkan foto profil dengan memprioritaskan kolom `mahasiswa.foto` / `mahasiswa.foto_url` dari database backend website agar sinkronisasi data foto profil dari website ke mobile berjalan sempurna dan real-time.
- **Mentor Kencana (Perbaikan Layout BkuAppBar):** Memperbaiki eror layout runtime (`RenderConstrainedBox expected a child of type RenderBox but received a child of type _RenderSliverPinnedPersistentHeaderForWidgets`) saat mengakses tab "Mentee", "Absensi", "Penilaian", serta halaman sub-fitur lainnya. Eror terjadi karena `BkuAppBar` yang menghasilkan widget bertipe `SliverAppBar` diletakkan di dalam parameter `appBar` milik `Scaffold` yang hanya menerima widget berbasis box biasa. Semua halaman tersebut (termasuk `mentor_mentee_screen.dart`, `mentor_scoring_screen.dart`, `mentor_recruit_screen.dart`, `mentor_absence_requests_screen.dart`, `mentor_attendance_screen.dart`, dan `mentor_mentee_detail_screen.dart`) telah direfaktor untuk menggunakan `CustomScrollView` atau `NestedScrollView` sehingga `BkuAppBar` diletakkan langsung di dalam daftar `slivers` secara benar dan kokoh sesuai protokol tata letak Flutter.
- **Mentor Kencana (Dashboard Real-Time Polling & Clean HTML):** Menambahkan sistem sinkronisasi data real-time pada dashboard Mentor Kencana dengan background polling setiap 3 detik (memanggil `fetchDashboard(silent: true)`), sehingga perubahan data mentee, grup, absensi, atau pengumuman yang dilakukan oleh Super Admin di panel web langsung terupdate secara instan pada aplikasi mobile tanpa memerlukan interaksi manual. Selain itu, menambahkan parser regex untuk membersihkan tag HTML (seperti `<p>`, `</p>`, `&nbsp;`) pada isi pengumuman sehingga teks tampil rapi dan bersih di layar handphone.
- **Sesi Pengguna (Persistensi Peran Terpilih):** Memperbaiki bug di mana pengguna yang memilih peran Mentor Kencana (atau peran non-mahasiswa lainnya) selalu mental kembali (revert) ke peran Mahasiswa setelah aplikasi dimuat ulang/direstart (hot restart / hot reload). Merefaktor method `loadSession()` di `AuthService` untuk memprioritaskan kunci `user_role` yang tersimpan di `SharedPreferences` sebelum membaca dari data profil default di `user_data`. Selain itu, memperbarui logika pengalihan rute pada `splash_screen.dart` agar mengarahkan pengguna dengan peran `UserRole.mentorKencana` secara benar ke halaman dashboard utama Mentor Kencana (`AppRoutes.mentorKencanaMain`) bukannya mental ke dashboard Mahasiswa.
- **Konseling (Psikolog Real-Time Polling):** Added periodic polling timers (every 3 seconds) to the Psychologist's "Dashboard" (`PsychologistDashboardScreen`), "Booking Masuk" (`PsychologistBookingsScreen`), and "Daftar Pasien" (`PatientListScreen`) screens to refresh data in the background. Updated `loadDashboardData`, `loadBookings`, and `loadPatients` in `PsychologistDashboardProvider` and `CounselingProvider` to support silent reloading (`silent: true`) to prevent screen flashing, shimmers, or layout flickering during background updates.
- **Konseling (Psikolog Dashboard Stats):** Ensured the data mapped into the "QuickStatsCard" in the psychologist's dashboard updates in real-time by polling the dashboard data in the background every 3 seconds. The original layout and styling are kept exactly as is on mobile.
- **Konseling (Psikolog Upcoming Appointments Filter):** Filtered the "Jadwal Mendatang" (Upcoming Appointments) list on the psychologist's dashboard to include both `'Menunggu'`/`'pending'` (waiting approval) and `'Dikonfirmasi'`/`'confirmed'` (approved) bookings. Handled sessions (status `'Selesai'`/`'completed'`) or rejected sessions (status `'Ditolak'`) will automatically disappear from the dashboard list instantly.
- **Sesi Pengguna (Real-Time Profile Photo & State Sync):** Upgraded **`AuthService`** to extend **`ChangeNotifier`** and registered it globally within **[main.dart](file:///Users/agam/Desktop/ubkmobail/lib/main.dart)**: (1) Added `notifyListeners()` triggers inside `updateUserData()` and `fetchMe()` in **[auth_service.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/services/auth_service.dart)**; (2) Refactored both **[mentor_dashboard_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_dashboard_screen.dart)** and **[mentor_profile_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_profile_screen.dart)** to listen to `AuthService` dynamically using `context.watch<AuthService>()`. Whenever a user uploads a new profile picture, the change is propagated instantly across all active widgets (such as the app bar avatar) in real-time.
- **Sesi Pengguna (Dynamic Base URL & Avatar Cache-Busting):** Fixed a critical backend discrepancy where the app bar and profile photo failed to load on production/test environments, falling back to initials ("MA"). Replaced the legacy hardcoded domain `https://kencana.unbaja.ac.id/api` in **[student_provider.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/providers/student_provider.dart)**, **[mentor_dashboard_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_dashboard_screen.dart)**, and **[mentor_profile_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_profile_screen.dart)** with `ApiGate.baseUrl` dynamically, allowing the app to resolve relative image paths (`/storage/avatars/...`) against the active server (e.g., `tukang.bkustudenthub.com`) correctly. Also implemented a global **`avatarCacheBuster`** in **[api_gate.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/services/api_gate.dart)** which automatically appends a timestamp query parameter (`?v=timestamp`) to all avatar/photo network image requests. By calling `ApiGate.refreshAvatarCache()` inside `fetchMe()`, the app instantly bypasses Flutter's network image cache and loads the new photo whenever the user profile is refreshed or modified on either the web interface or the mobile app.
- **Sesi Pengguna (Avatar Persistence Fix):** Fixed a bug where uploading a profile picture successfully would revert back to the initials avatar ("MA") upon page reload or app restart. Updated both **[student_provider.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/providers/student_provider.dart)** and **[psychologist_dashboard_provider.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/providers/psychologist_dashboard_provider.dart)**'s avatar upload methods to write and persist the new photo URL into `AuthService().userData` and SharedPreferences (`user_data` cache) immediately upon a successful upload.
- **Mentor Kencana (Premium Profile & Settings):** Upgraded the **[mentor_profile_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_profile_screen.dart)** into a `StatefulWidget`: (1) Added an interactive profile picture card showing the network profile image (using `Image.network` and `ApiGate.getImageUrl`) with initials as fallback; (2) Configured the avatar to be read-only and dynamically synchronized with the Student profile photo to prevent backend discrepancies; (3) Configured the **`BkuAppBar`** on **[mentor_dashboard_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mentor_kencana/presentation/pages/mentor_dashboard_screen.dart)** to load the mentor's profile image dynamically; (4) Kept the brand-themed app bar and settings/logout integrations fully intact; (5) Integrated **`AuthService().fetchMe()`** inside the refresh actions of both the profile and dashboard screens to automatically fetch the latest user info (including profile photos) from the backend whenever the user pulls to refresh.
- **Mentor Kencana (Dashboard Navigation & Service Menu):** Added direct navigation links and interactive features to make the mobile Mentor Kencana experience match the website exactly: (1) Added a new **"Layanan Kencana"** (Service Menu) section on the dashboard with quick action cards for **"Rekrut Mentee"** (navigates to the recruitment screen) and **"Surat Izin Mentee"** (navigates to the absence requests screen); (2) Made the four statistical cards ("Total Mentee", "Grup Aktif", "Belum Dinilai", "Notifikasi") fully clickable to navigate to their corresponding sub-tabs or screens instantly.
- **Mentor Kencana (Robust API Keys & Data Parsing):** Fixed a bug where the dashboard stats and announcements cards on the Mentor Kencana screen appeared empty or as zero. Resolved this by (1) adding robust support for both English and Indonesian JSON keys (`judul`/`title`, `isi`/`content`/`deskripsi`, `tanggal`/`created_at`, `total_mentee`, `belum_dinilai`, etc.) in all domain models under `mentor_models.dart`, and (2) making the `MentorKencanaProvider` API fetching functions resilient by handling variations in payload wrapping.
- **Konseling (Psikolog Upcoming Appointments Buttons):** The button on the appointment card for pending/menunggu bookings now displays an active **"Konfirmasi"** action button. This button (along with the "Tangani" button) now dynamically inherits the brand primary color (`context.watch<ThemeProvider>().primary`) configured by the Super Admin (matching the website brand configurations like orange `#F1762F` exactly). Clicking this button sets the tab index to the "Menunggu" tab and redirects to the bookings screen. Confirmed bookings display the "Tangani" button, which redirects to the bookings screen under the "Dikonfirmasi" tab.
- **Konseling (Psikolog Dashboard Real-Time Synchronization):** Fixed a bug where accepting/confirming bookings or submitting session notes did not immediately update the dashboard stats ("Menunggu" and "Janji Temu Disetujui") in real-time. Added immediate dashboard data reloading (`PsychologistDashboardProvider.loadDashboardData()`) to the success callbacks of booking status updates and session note submissions.
- **Konseling (Psikolog Booking Tab Filtering):** Fixed a major bug where pending bookings (with status `'pending'`) were not displayed under the "Menunggu" tab of the bookings screen due to case-sensitive and string label mismatch. Tab status filtering is now case-insensitive and mapped correctly to support both English (`pending`, `confirmed`, `completed`) and Indonesian (`menunggu`, `dikonfirmasi`, `selesai`) values. Automatically reload bookings list from server in `CounselingProvider.updateBookingStatus()` to guarantee consistency.
- **Konseling (Psikolog Selesai Action Button):** Added a `'Selesai'` action button to the bottom sheet of confirmed bookings under the "Dikonfirmasi" tab, matching the website. Clicking the `'Selesai'` button navigates the psychologist directly to the `SessionNoteScreen` (write session notes) for that booking.
- **Konseling (Psikolog Profile Photo Fix):** Fixed a bug where the psychologist's profile picture or initials avatar would appear completely empty/transparent on the dashboard. Resolved this by (1) adding all possible profile photo keys (`profile_image_url`, `avatar`, `profile_photo`, `foto_profil`) to the `Psychologist` model parser, and (2) removing a redundant `FittedBox` wrapper inside the collapsed state of `BkuAppBar` which scaled container-based avatars (like the initials placeholder) down to `0x0` size.
- **Konseling (Psikolog Session Note Brand Color):** Changed the generic green color (`Colors.green`) of the toggle buttons ("Ya" options) and warning indicators in the `SessionNoteScreen` to use the dynamic primary brand color (`context.watch<ThemeProvider>().primary`) configured by the Super Admin (matching the website brand configurations like orange `#F1762F` exactly).
- **Konseling (Psikolog Session Note Status):** Fixed a bug where creating a session note did not update the booking status to `'Selesai'` if the treatment was marked as not finished (`tindak_lanjut_tuntas = false`). The booking status is now always set to `'Selesai'` upon session note submission to ensure the handled session disappears from "Jadwal Mendatang" instantly.
- **Konseling (Psikolog Dashboard Label Clarification):** Replaced the label "Janji Temu Hari Ini & Mendatang" with "Janji Temu Disetujui" and the description "sesi konseling hari ini dan yang akan datang" with "sesi konseling yang telah disetujui" to remove ambiguity and match the website's confirmed status semantics.
- **Konseling (Psikolog Dashboard Button Navigation):** Fully implemented navigation actions for all buttons/items in the "QuickStatsCard" on the psychologist's dashboard. Clicking the header (Janji Temu Disetujui) navigates to "Booking Masuk" under the "Dikonfirmasi" sub-tab; clicking "Selesai Hari Ini" or "Selesai Bulan Ini" navigates to the "Selesai" sub-tab; and clicking "Menunggu" or "Baru Hari Ini" navigates to the "Menunggu" sub-tab. Enabled state synchronization between `NavigationProvider` and `PsychologistBookingsScreen`.
- **Konseling (Psikolog Back Button Navigation):** Fixed the back button `<` in the app bars of "Booking Masuk" (`PsychologistBookingsScreen`), "Daftar Pasien" (`PatientListScreen`), and "Pengaturan" (`PsychologistSettingsScreen`) screens doing nothing when tapped. These screens now accept an optional `onBack` callback from the root `PsychologistMainScreen` to switch tabs back to the Dashboard (Index 0). If they are opened as standalone pushed screens (e.g. from the Dashboard Grid), they fallback to popping the route via GoRouter `context.pop()`.
- **App Bar (Global Back Button Navigation):** Fixed the back button `<` in `BkuAppBar` and `BkuStaticAppBar` failing to pop top-level GoRouter routes (such as schedule management, referrals, and analytics) due to Navigator/GoRouter stack mismatches. It now checks `context.canPop()` and executes `context.pop()` for clean route popping, falling back to `Navigator.maybePop()` if needed.
- **Auth (Double Degree):** Fixed double-degree student role selection failing to show on mobile. Updated `AuthService` to dynamically detect when `mahasiswa` records are returned as a List/Array, and properly map them into the Bottom Sheet selection.
- **Auth (Double Degree Select Role):** Fixed select-role payload structure. The backend select-role API expects the selected role to be `"student"` and the selected NIM to be passed under the key `"selected_nim"` (instead of merging them or sending only the NIM as the role name).
- **Network (Multi-Tenancy Context):** Modified `ApiInterceptor` to inject `Origin: https://bkustudenthub.com` into all outgoing API requests. This ensures the backend correctly routes mobile requests to the school's tenant database context, allowing it to return multiple NIMs for double-degree accounts just like on the web.

## [Unreleased] - 2026-07-11
### Added
- **Psychologist:** Fixed PDF download buttons in Referral Management and Patient List screens. Replaced `launchUrl` with direct byte downloads using `ApiClient().client` and `Printing.sharePdf` to correctly inject auth tokens. Implemented explicit error handling to display 500 Internal Server Error details when the backend crashes (e.g. missing asset or index out of range).
- **Psychologist:** Fixed the avatar upload bug where the new profile picture would not render immediately. Modified `uploadAvatar` to also hit `PUT /psychologist/profile` to update the local database cache, and used `avatarVersion` to bust the network image cache.
- **Psychologist:** Redesigned Alert Dialogs for Avatar Upload Success/Failure and Logout in the Profile/Settings screen to match the modern circular-icon design of the Student role.
- **Psychologist:** Cleaned up unused imports and `0 issues found` warnings across the psychologist counseling feature.

- **Auth:** Refined dynamic multi-role parsing (such as double degrees) to match the Web portal design. Labels are formatted as "Mahasiswa (NIM)" if a student NIM is present. Descriptions are dynamically constructed from "prodi" and "kampus" if empty. Proper fallback icons and color palettes (e.g. BKU Blue for student roles, green for mentors) are automatically applied depending on the role key.
- **Auth:** Implemented fully dynamic parsing for multi-role login selections (such as double degrees or mentor roles) returned from the backend. Student role keys are dynamically appended with their corresponding NIM (e.g. `student-AK116001`) to ensure uniqueness in the Bottom Sheet list. Upon selection, the NIM is parsed and sent to `/auth/login/select-role`, and forcefully updated in the local user data cache to load the correct student profile.
- **Auth:** Fixed a bug in the mock fallback for multi-role login (`loginSelectRole`) where selecting a different student role (e.g., `AK116001`) would incorrectly load the profile of the first role (`201FK04002`). The `auth_service.dart` now correctly extracts the chosen NIM from the selected role string and forcefully overwrites the `nim` in local `_userData` cache, ensuring `StudentProvider` loads the correct specific student profile upon redirection.
- **Auth:** Added simulated multi-role selection for specific test users to test the Bottom Sheet UI when backend roles are not yet provided.
- **Auth:** Standardized the `SnackBar` alert design in `login_screen.dart` (for empty fields, connection timeout, failed login) to use the floating design (`SnackBarBehavior.floating`) with dynamic theme colors, matching the student role design reference.
- **Auth:** Refactored the login multi-role selection to be fully dynamic, removing all hardcoded email simulation blocks (such as for `ahelmikurniawan98@gmail.com`, `mentor@gmail.com`, and `multi@gmail.com`). The app now parses and maps the roles returned by the backend dynamically. Each role is safely structured to ensure the UI has the required fields (`role`, `label`, `description`, `icon`, `color`) without hardcoding, allowing proper mapping of user inputs (like NIMs) to the backend.
- **Splash Screen:** Removed the hardcoded deep navy blue background color (`0xFF0F172A`) on `splash_screen.dart` and `login_screen.dart`, substituting it with the dynamic `Theme.of(context).colorScheme.surface` to properly adopt the background colors defined by the Super Admin's dynamic branding.
- **Profile:** Refactored the Logout confirmation in `tk_settings_screen.dart` to use the standard student role `showLogoutDialog(context)` instead of a custom bottom sheet. Fixed dynamic primary colors mapped to Orange inside the dialog to be explicitly Black (`Color(0xFF111827)`) as requested for text and outlined buttons.
- **Tenaga Kesehatan:** Redesigned `tk_settings_screen.dart` (profile & settings page) to match the exact visual style, colors, and layout of the Student profile page (`ProfileScreen`). Includes a clean white-card role card (border-radius 24), a dedicated data section, structured notification switches, and a custom logout button.
- **Tenaga Kesehatan:** Reverted the header gradients, button backgrounds, and SnackBars to be dynamically themed using the role's dynamic color configuration (orange) instead of hardcoding to blue, while keeping the student's structural layout.
- **Tenaga Kesehatan:** Configured all Tenaga Kesehatan screen headers (`BkuAppBar` and `BkuStaticAppBar` under `AppBarVariant.nakes`) to use the dynamic theme gradient, showing the Title and Notification button (`showNotification: true`). Removed the Back button (`showBackButton: false`) on the Dashboard to prevent navigation confusion at the root level.
- **Tenaga Kesehatan:** Disabled `showBackButton` for all sub-pages used as tabs under `tk_main_screen.dart` (`TkScheduleScreen`, `TkPatientListScreen`, and `TkSettingsScreen`) to avoid unexpected app exit/navigation when tapping back on top-level tabs.
- **Tenaga Kesehatan:** Fixed the `ListTile` DecoratedBox background assertion warning by wrapping settings tiles and switch tiles inside a transparent `Material` widget.
- **Tenaga Kesehatan:** Resolved the `RenderImage object was given an infinite size during layout` crash by removing infinite dimension constraints from `profileImage` network image in `tk_dashboard_screen.dart`.
- **Tenaga Kesehatan:** Aligned the SnackBar alert design inside `tk_settings_screen.dart` to use the floating card design (without icons, using theme colors) matching the Student profile screen alerts.
- **Tenaga Kesehatan:** Fixed image cache-refreshing by appending a cache-busting timestamp parameter directly to the final parsed URL after calling `ApiGate.getImageUrl`, ensuring the new avatar displays instantly in the UI upon successful upload.
- **Tenaga Kesehatan:** Added automatic cache updates to `AuthService().userData` upon successful profile data editing (Nama, Email, No. HP, Lokasi) to prevent local settings page from falling back to old data.

### Changed
- **Tenaga Kesehatan:** Redesigned `tk_patient_detail_screen.dart` (patient detail screen) to match the Student profile (`ProfileScreen`) design system, featuring a floating card layout, circular avatar structure, dynamic badges for NIM and blood group, and structured menu sections with icons.
- **Ormawa:** Standardized header in `ormawa_recruitment_screen.dart` to use `BkuStaticAppBar` for visual consistency across roles.
- **Profile:** Connected the "Test Push Notifikasi" button to actual `LocalNotificationService.showNotification` instead of a dummy SnackBar.

### Removed
- **Organisasi:** Removed unused dummy "Dokumentasi Kegiatan" grid from `organisasi_screen.dart` to clean up the UI.

### Fixed
- **Tenaga Kesehatan:** Reverted all primary UI elements (selection titles, filter chips, export/download buttons, focused borders, tab indicators, details modal icons) across clinical reports (`tk_clinical_reports_screen.dart`), patient list (`tk_patient_list_screen.dart`), patient card initials (`tk_patient_card.dart`), patient detail (`tk_patient_detail_screen.dart`), booking screen (`tk_booking_screen.dart`), and insurance claims (`tk_insurance_claims_screen.dart`) to read directly from `ThemeProvider` instead of `Theme.of(context).colorScheme.primary`, ensuring the correct dynamic theme color (Orange) is used in sub-routes/dialogs/bottom-sheets instead of falling back to the global default Blue color.
- **Tenaga Kesehatan:** Redesigned the insurance claim review bottom sheet in `tk_insurance_claims_screen.dart` to directly display claim attachments using network image previews with secure auth tokens, alongside showing complete details (student info banner, detail rows, chronology box) to make reviews fully complete.
- **Tenaga Kesehatan:** Redesigned the student list screen (`tk_patient_list_screen.dart`), the selection bottom sheets, and the initials avatar in `tk_patient_card.dart` to use static BKU Blue colors (`Color(0xFF002068)`), a 16pt card border radius, and a 32pt sheet top radius, ensuring the visual styling aligns with the Student role's reference design.
- **Tenaga Kesehatan:** Redesigned the insurance claim card and review bottom sheet in `tk_insurance_claims_screen.dart` to match the Student role's visual design, including info rows with custom icons, clean status badge styling with translucency, and input styling with BKU Blue focused borders.
- **Tenaga Kesehatan:** Redesigned the Edit Profile bottom sheet and Change Password dialog in `tk_settings_screen.dart` to use static BKU Blue colors (`Color(0xFF002068)`) for headers, buttons, and textfield focused borders instead of dynamic theme colors, preventing any orange visual indicators from showing up in these popups, matching the Student role's visual palette.
- **Tenaga Kesehatan:** Replaced dynamic text/foreground and cancel button outline colors with static colors (neutral gray and BKU Blue) across settings, booking list, manual input dialog, screening input, and patient detail screens to prevent them from flashing or defaulting to the dynamic orange theme color.
- **Mentor:** Resolved compile-time warnings and TODOs in `mentor_dashboard_screen.dart` to maintain a zero-warning project state.
- **Tenaga Kesehatan:** Aligned the design of the Edit Profile (bottom sheet layout with independent labels, color-coded prefix icons, and borderRadius 32) and Change Password (now a native AlertDialog) in `tk_settings_screen.dart` with the reference design from the Student role.
- **Tenaga Kesehatan:** Fixed the issue where all profile and patient pictures in the Tenaga Kesehatan role were not rendering by updating `TkProfile.fromJson`, `Patient.fromJson`, and `Booking.fromJson` to use a unified, robust IIFE loop parser that parses alternative image keys (`avatar`, `foto`, `avatar_url`, `foto_url`, `FotoURL`) and nested structures (`user`, `mahasiswa`) matching the Student role's parsing configuration.
- **Tenaga Kesehatan:** Fixed the student profile picture not showing in the booking list in `tk_booking_screen.dart` by expanding the `Booking.fromJson` parsing logic in `booking.dart` to robustly check alternative keys (`avatar_url`, `foto`, `FotoURL`, `avatar`) and nested structures under the `mahasiswa` and `user` maps.
- **Tenaga Kesehatan:** Fixed the practice schedule deletion bug in `tk_schedule_screen.dart` where confirming deletion did not trigger the deletion action due to `Navigator.pop(context)` not returning `true` inside the confirmation `CustomDialog` callbacks.
- **Tenaga Kesehatan:** Reverted profile update and change password endpoints back to the role-specific `/tenagakes/profile` and `/tenagakes/change-password` in `TkRepositoryImpl` because the unified `/profil/data-diri` and `/profil/change-password` endpoints return 403 Access Denied for the Tenaga Kesehatan role.
- **Tenaga Kesehatan:** Fixed the "Akses Ditolak" (Access Denied) permission error during profile picture change by removing the redundant call to `repository.updateProfile` in `uploadProfileAvatar`, relying instead on the backend's automatic update of the user's avatar on upload and our robust `TkProfile.fromJson` fallback to correctly parse it upon refresh.
- **Tenaga Kesehatan:** Fixed the availability toggle button reverting issue by incorporating a click guard (`_isUpdating`), performing optimistic UI updates, sending comprehensive backend status keys (`is_aktif`, `IsAktif`, `is_active`, `is_available`), and preserving the updated status locally on the profile object instead of letting it default to true in the `/profil/data-diri` response.
- **Tenaga Kesehatan:** Fixed profile photo update not persisting on backend by sending `updateProfile` data after uploading, awaiting `AuthService().updateUserData`, and making `TkProfile.fromJson` parsing robust to fall back properly to `userMap` even if outer `foto_url` is an empty string.
- **Tenaga Kesehatan:** Fixed the layout assertion crash when opening the Booking tab by replacing illegal layout-phase `context.watch<ThemeProvider>()` calls inside list builders with layout-safe `context.read<ThemeProvider>()` calls, while registering the watch callback safely at the top of the main `build` method.
- **Tenaga Kesehatan:** Fixed the app bar's PDF download button on the Patient Detail screen (`tk_patient_detail_screen.dart`) to request the latest medical record ID instead of the patient ID, preventing the "Medical record not found" API error.
- **Navigation:** Fixed the app closing bug on the Tenaga Kesehatan settings page by overriding the app bar's back button callback to navigate back to tab 0 (Dashboard) instead of triggering a fallback `Navigator.maybePop(context)`.
- **Navigation:** Respect the `showBackButton` parameter in `TkSettingsScreen`'s `BkuAppBar` instead of hardcoding it to `true`.
- **Login:** Reset the global `NavigationProvider` index to `0` when logging in for any user role to prevent out-of-bounds crashes in `IndexedStack` (e.g. when logging in as a psychologist after logging out of a student profile page on tab index 4).
- **Login:** Interceptor handling for 401 on `/auth/login` to show actual error messages instead of forcing a session expiration.
- **Login (Role Selection):** Enforced multi-role handling so users with dual roles (e.g., S1 and Profesi Ners) are properly redirected to the role selection screen instead of bypassing it.
- **Profile:** Extracted `avatar_url` correctly in `StudentRepository` and `StudentProvider` so profile avatars load successfully upon login.
- **Profile:** Added missing `UCropActivity` to `AndroidManifest.xml` to prevent crashes when cropping the profile picture.
- **Profile:** Restyled `ProfileScreen` header to use `BkuAppBar` (`AppBarVariant.student`) to maintain design consistency with Dashboard and Scholarship screens.
- **Profile:** Fixed Alamat Domisili menu tap to display the uneditable info dialog (with the Aspirasi button) instead of an error or incorrect form.
- **Profile:** Fixed `birthPlaceDate` formatter to correctly display the birth date even if the birth place data is empty from the API.
- **Data:** Implemented comprehensive fallback keys (`ProgramStudi`, `Prodi`, `jurusan`) for retrieving Program Studi to ensure the data is displayed correctly on the role card.
- **Klinik (Student):** Broadened the fallback keys for avatar parsing (`picture`, `photo`, `image`, `gambar`, `profile_picture`) in `HealthWorkerModel` to ensure health worker photos load correctly matching the web version.
- **Klinik (Student):** Fixed timezone parsing issue in `HealthScheduleModel` by adding `.toLocal()` conversion, preventing Saturday schedules from being incorrectly shifted to Friday and hidden from the UI.
- **Health Dashboard:** Wrapped the 3 "Langkah Mudah Memulai" instruction steps in `HealthScreen` with `InkWell` and assigned appropriate `onTap` navigations to make them functional.
- **Tenaga Kesehatan:** Fixed profile update endpoint from `PUT /tenagakes/profile` to `PUT /profil/data-diri` in `TkRepositoryImpl`.
- **Tenaga Kesehatan:** Fixed change password endpoint from `PUT /tenagakes/change-password` to `PUT /profil/change-password` in `TkRepositoryImpl`.
- **Tenaga Kesehatan:** Fixed photo/avatar upload request parameter from `'avatar'` to `'foto'` in `TkRepositoryImpl` and `CounselingRepositoryImpl` to match the backend multipart contract and resolve the *"File tidak ditemukan"* upload error.
## [11 Juli 2026 - 04:10 WIB]

### Diperbaiki (Fixed)
- **Perbaikan Warna dan Responsivitas Toggle Ketersediaan Psikolog (Availability Toggle Fix)**:
  - Mengubah warna visual status "Sibuk" pada [availability_toggle.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/widgets/dashboard/availability_toggle.dart) menjadi warna merah (`Color(0xFFEF4444)`) untuk memberikan indikator visual yang kontras dan intuitif bagi psikolog, serta memperbarui ikon penanda dari daya menjadi ikon silang (`Icons.close_rounded`) berwarna merah. Status "Tersedia" tetap menggunakan warna hijau (`Color(0xFF10B981)`) dengan ikon centang (`Icons.check_rounded`).
  - Menambahkan `behavior: HitTestBehavior.opaque` pada gesture detector toggle untuk meningkatkan akurasi dan kecepatan respon deteksi sentuhan pengguna di dalam flexible space AppBar.
  - Mengimplementasikan klik guard (`if (_isUpdating) return;`) pada `toggleAvailability()` di [psychologist_dashboard_provider.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/providers/psychologist_dashboard_provider.dart) untuk mencegah *race condition* dan kedipan UI ketika pengguna melakukan klik ganda secara cepat ketika pembaruan status ke backend sedang berjalan.
  - Memperbarui objek model `_profile` lokal psikolog secara dinamis ketika pembaruan berhasil untuk menjamin status ketersediaan tersinkronisasi di seluruh komponen UI yang membaca dari model profil.
  - Memperluas payload request pada `updateAvailability()` di [counseling_repository_impl.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/data/repositories/counseling_repository_impl.dart) dengan menyertakan seluruh kombinasi kunci ketersediaan (`is_available`, `is_aktif`, `is_active`, `IsAktif`) untuk menjamin kecocokan pembaruan kolom status ketersediaan pada database backend.
- **Penyesuaian Ukuran Ikon Dashboard (Enlarge Dashboard Icons)**:
  - Mengubah ukuran ikon ringkasan hari ini (Selesai Hari Ini, Menunggu, dll.) pada [quick_stats_card.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/widgets/dashboard/quick_stats_card.dart) dari **16dp** menjadi **24dp** dan menyesuaikan padding lingkaran menjadi **10dp** untuk visualisasi yang lebih jelas dan profesional.
  - Mengubah ukuran ikon tipe sesi pada [upcoming_appointments_card.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/widgets/dashboard/upcoming_appointments_card.dart) dari **14dp** menjadi **18dp**, padding lingkaran menjadi **10dp**, serta ukuran ikon jadwal jam kecil dari **12dp** menjadi **15dp**.
  - Mengubah ukuran ikon aktivitas pada [recent_activities_card.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/widgets/dashboard/recent_activities_card.dart) dari **16dp** menjadi **20dp** dengan padding lingkaran menjadi **10dp**.

## [11 Juli 2026 - 02:30 WIB]

### Diperbaiki (Fixed)
- **Redesain Dialog Ubah Password (Change Password Dialog Redesign)**:
  - Memperbarui total desain visual dialog "Ubah Password" pada [profile_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/profile/presentation/pages/profile_screen.dart) agar terlihat premium, modern, dan rapi sesuai standar UI/UX BKU Hub.
  - Menambahkan ikon dekoratif reset kunci (`Icons.lock_reset_rounded`) dengan lingkaran latar belakang semi-transparan dari warna tema utama di atas judul dialog.
  - Mengubah ketiga input field (`TextField` Password Lama, Password Baru, dan Konfirmasi Password Baru) agar seragam menggunakan sudut melengkung 16dp (`BorderRadius.circular(16)`), dilengkapi ikon awalan (`prefixIcon`) yang sesuai, serta menghilangkan label terpotong.
  - Menata ulang tombol aksi (Batal & Simpan) menjadi berjajar horizontal (`Row` dengan `Expanded`) dengan tombol simpan memanfaatkan warna utama kontras-aman (`themeProvider.primary` dan `themeProvider.onPrimary`) serta tombol batal menggunakan gaya `OutlinedButton` abu-abu yang bersih.
- **Aktivasi Fitur Pencarian Konseling (Counseling Search Feature)**:
  - Memperbaiki tombol ikon pencarian (search) pada bilah aplikasi (*App Bar*) utama Layanan Konseling ([counseling_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/counseling/presentation/pages/counseling_screen.dart)).
  - Mengubah aksi tombol pencarian agar mengarahkan (*navigating*) secara langsung ke halaman daftar psikolog ([psychologist_list_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/counseling/presentation/pages/psychologist_list_screen.dart)) dengan mengirimkan parameter `autoFocusSearch: true`.
  - Menambahkan dukungan `FocusNode` pada bilah pencarian daftar psikolog untuk memunculkan papan ketik (*autofocus keyboard*) secara otomatis ketika dibuka melalui tombol pencarian utama, guna meningkatkan pengalaman pengguna (UX).
- **Validasi ID Psikolog Saat Booking Konseling (Booking Submission)**:
  - Memperbaiki kegagalan pengiriman pesanan konseling (*stuck* dengan pesan kesalahan "psikolog_id wajib diisi") pada [counseling_booking_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/pages/counseling_booking_screen.dart).
  - Mengubah ekstraksi `psikolog_id` dan `slot_id` dari objek slot terpilih secara dinamis tanpa pemaksaan konversi ke `int`. Hal ini untuk mendukung format ID asli berupa string/UUID dari database backend.
  - Menambahkan alur penyelesaian ID psikolog secara bertingkat (check keys case-insensitive pada slot root level, nested psychologist/psikolog, widget parameter, dan detail provider) untuk menjamin parameter ID terkirim dengan benar ke backend.
- **Konsistensi Desain Notifikasi SnackBar & Warna Status (Design & Theme Consistency)**:
  - Memperbaiki seluruh SnackBar notifikasi sukses dan error pada halaman pendaftaran/pembatalan konseling dan klinik ([counseling_booking_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/counseling/presentation/pages/counseling_booking_screen.dart), [health_booking_form_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/health/presentation/pages/health_booking_form_screen.dart), dan [klinik_booking_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/health/presentation/pages/klinik_booking_screen.dart)).
  - Memperbaiki seluruh SnackBar sukses dan error pada halaman profil ([profile_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/profile/presentation/pages/profile_screen.dart)) untuk ganti foto profil, perbarui profil biodata, simpan preferensi notifikasi, dan ubah kata sandi agar seragam menggunakan desain melayang, sudut membulat 12dp, latar belakang dinamis brand (`themeProvider.primary` / `themeProvider.colorError`), serta teks kontras yang sesuai.
  - Mengubah semua notifikasi agar seragam menggunakan gaya melayang (`behavior: SnackBarBehavior.floating`) dengan sudut membulat 12dp (`shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))`).
  - Mengganti penggunaan warna statis merah (`Colors.red`) atau warna bawaan tema (`Theme.of(context).colorScheme.error`) pada SnackBar error dengan warna dinamis `themeProvider.colorError` yang sinkron dengan preferensi Super Admin di backend.
  - Memetakan latar belakang SnackBar sukses ke warna utama brand mobile (`themeProvider.primary`) yang dapat dikonfigurasi penuh dari Super Admin (misalnya warna `#F1762F` oranye).
  - Mengubah getter `onPrimary` dan `onSecondary` pada [mobile_theme.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/theme/mobile_theme.dart) menjadi dinamis menggunakan kalkulasi kontras luminansi (`_getOnColor`). Hal ini otomatis menentukan warna teks/konten di atas latar belakang primary (putih atau hitam pekat) agar lulus uji kontras aksesibilitas WCAG (AAA).
  - Menerapkan warna teks dinamis `themeProvider.onPrimary` pada seluruh SnackBar sukses untuk memastikan konten tetap terbaca dengan jelas (misal teks hitam saat latar belakang oranye).
  - Mengganti warna hardcoded `Colors.green` dan `Colors.red` pada indikator slot (sisa kuota, badge penuh, linear progress indicator) di formulir booking klinik dengan warna dinamis `themeProvider.success` dan `themeProvider.colorError`.
  - Menghubungkan warna tombol pembatalan dan catatan review di detail antrean klinik ([klinik_booking_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/health/presentation/pages/klinik_booking_screen.dart)) dengan warna dinamis `themeProvider.colorError`.
- **Perbaikan Alur Pengiriman Aspirasi & Warna Notifikasi (Student Voice Fixes)**:
  - Memperbaiki bug *stuck loading* (looping) saat mengirim aspirasi di [submit_aspiration_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/student_voice/presentation/pages/submit_aspiration_screen.dart) dengan memanggil `BkuLoadingDialog.hide(context)` secara eksplisit sebelum menampilkan dialog sukses/error.
  - Memperbaiki warna notifikasi in-app SnackBar ketika menerima pemberitahuan di latar depan (*foreground*) pada [notification_service.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/services/notification_service.dart). Notifikasi bertipe sukses kini diwarnai menggunakan warna utama brand (`theme.primary`, misalnya oranye `#F1762F`) alih-alih hijau statis (`theme.success`), dengan teks yang menyesuaikan kontras secara otomatis (`theme.onPrimary`).
  - Mengintegrasikan dynamic branding ke ikon sukses dan tombol "Kembali" pada dialog sukses pengiriman aspirasi agar konsisten dengan warna utama dari Super Admin.
  - Memperbaiki penulisan teks pemberitahuan pada [notification_service.dart](file:///Users/agam/Desktop/ubkmobail/lib/core/services/notification_service.dart). Karakter tanda kurung dan kutip tunggal mentah database `('...')` kini otomatis digantikan dengan format tanda kutip ganda yang lebih rapi `("...")` di level *JSON parsing* Model `NotificationItem`, sehingga seluruh riwayat notifikasi, in-app SnackBar, dan sistem notifikasi tampil lebih bersih dan profesional.
- **Peta Label Status Klaim Asuransi (Insurance Claim Status Badge)**:
  - Memperbaiki kesalahan *mapping* label status klaim asuransi pada [insurance_claim_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/health/presentation/pages/insurance_claim_screen.dart).
  - Menyelesaikan masalah di mana status `PENDING_VERIFICATION` tampil mentah sebagai teks kapital database (`PENDING_VERIFICATION`). Logika pencocokan kini secara tepat memetakan status `PENDING_VERIFICATION` ke label Bahasa Indonesia `"Menunggu Verifikasi"`, agar konsisten dengan standar tampilan status lainnya.
- **Validasi Akses Peran & Filter Akun Superadmin (Clinic Booking Security)**:
  - Memperbaiki kebocoran data akun administratif pada fitur booking klinik di [student_repository_impl.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/data/repositories/student_repository_impl.dart).
  - Melakukan penyaringan (*filtering*) otomatis pada tingkat repositori (`getHealthWorkers` dan `getHealthSchedules`) untuk mengeluarkan akun yang memiliki nama atau email mengandung unsur "superadmin" (case-insensitive). Langkah ini menjamin akun administratif Superadmin tidak akan pernah muncul sebagai tenaga kesehatan yang aktif ataupun memiliki jadwal pemeriksaan/konsultasi yang dapat dipilih/dipesan oleh mahasiswa.
  - Memperbaiki kegagalan perenderan foto bukti/lampiran aspirasi (*PathNotFoundException*) pada [student_voice_screen.dart](file:///Users/agam/Desktop/ubkmobail/lib/features/mahasiswa/student_voice/presentation/pages/student_voice_screen.dart). Alur logika perenderan gambar kini menggunakan helper dinamis `_isLocalFile` untuk memverifikasi apakah jalur file benar-benar merupakan file lokal di penyimpanan perangkat sebelum menggunakan `Image.file`, sehingga jalur file relatif remote dari server (seperti `/uploads/aspirasi/`) secara tepat dimuat melalui jaringan menggunakan `Image.network`.
  - Mengubah skala perenderan foto bukti aspirasi pada detail dialog dari `BoxFit.cover` menjadi `BoxFit.contain` dan memberikan latar belakang `surfaceContainerLow` pada container gambar. Hal ini mencegah gambar terpotong atau ter-*zoom* secara berlebihan, sehingga isi dokumen/foto bukti tetap terbaca dengan jelas.



## [11 Juli 2026 - 00:30 WIB]

### Diperbaiki (Fixed)
- **Tema & Branding Dinamis (Mobile Theme)**:
  - Memperbaiki pencocokan warna gradien default pada parsing JSON `MobileThemeColors.fromJson` agar tidak sensitif terhadap huruf besar/kecil (case-insensitive) sehingga warna fallback default gradien (`#00164e`) tidak merusak penyesuaian warna utama kustom yang diatur di Super Admin.
  - Memperbarui sistem fallback gradien pada tema mobile agar secara otomatis menggunakan warna solid dari `primary` dan `secondary` kustom apabila warna gradien tidak dikonfigurasi secara manual di Super Admin. Ini menghilangkan percampuran 2 warna yang tidak serasi (seperti hitam-oranye) pada komponen kartu/app bar.
- **Antarmuka & Keterbacaan (BkuAppBar & Font)**:
  - Mengubah warna font, ikon, tombol back, tombol notifikasi, dan dekorasi pada `BkuAppBar` dan `BkuStaticAppBar` agar menggunakan warna `onPrimary` yang kini dipetakan ke **Warna Permukaan Mobile (Surface)** dari Super Admin (berwarna putih/`#ffffff`), memastikan keterbacaan teks yang sempurna di atas latar belakang AppBar (warna utama/oranye) dan tidak berbenturan dengan warna sekunder (hitam).
  - Memetakan secara global `onSurface`, `onSurfaceVariant`, `outline`, dan `outlineVariant` di `ThemeData` agar merujuk langsung ke **Warna Kedua Mobile (Secondary)** dari Super Admin (berwarna hitam/`#000000`), menggantikan pembuatan warna teks otomatis bawaan Flutter yang sempat berwarna kecoklatan/tidak serasi.
  - Memperbaiki warna judul kegiatan/agenda dan berita pada kartu (seperti `TodayScheduleCard` dan `StudentAgendaList`) dari warna `primary` (oranye) menjadi `onSurface` (sekunder/hitam kustom dari Super Admin), memastikan seluruh teks mematuhi preferensi warna font dari Super Admin secara menyeluruh.
- **Splash Screen (Splash Screen Background & Text)**:
  - Mengubah warna latar belakang `SplashScreen` agar menggunakan **Warna Latar Mobile (Background)** dari Super Admin.
  - Teks, logo *shadow*, dan indikator pemuatan kini menggunakan **Warna Utama Mobile (Primary)**, menghasilkan tampilan *splash screen* yang elegan dan merepresentasikan warna merek utama institusi dengan presisi tinggi.
- **Pengaturan Preferensi Notifikasi**:
  - Memperbaiki fitur penyimpanan preferensi notifikasi (Notifikasi Push, Notifikasi Email, dan Peringatan In-App) agar tidak lagi bersifat statis/dummy. Preferensi kini tersimpan secara riil ke backend (BE) melalui pemanggilan `student.updateProfile` dan dipersistenkan di penyimpanan lokal `SharedPreferences` agar preferensi tetap konsisten ketika aplikasi dibuka kembali.
- **Tampilan Dokumen Prestasi (Achievement Certificate Preview)**:
  - Memperbaiki komponen *preview* foto sertifikat/piagam pada halaman `ReportAchievementScreen` agar tidak melar secara horizontal (*stretched/distorted*). Menggunakan wadah kontras bernuansa abu-abu terang (*Slate 50* - `#F8FAFC`) dengan tinggi 180dp dan *aspect ratio* yang dipertahankan menggunakan `BoxFit.contain` untuk menjaga keterbacaan penuh dokumen asli.
  - Memperbaiki kegagalan pembaruan *preview* foto sertifikat ketika prestasi diperbarui/diganti dengan file baru. Sebelumnya, `StudentProvider` hanya mengganti objek di memori secara lokal dengan URL sertifikat lama tanpa menarik URL hasil upload baru dari server. Kini, `addAchievement` dan `updateAchievement` secara otomatis memicu pemanggilan kembali `_repository.getAchievements()` setelah unggahan berhasil untuk menyinkronkan daftar data dan URL foto sertifikat yang baru secara instan di sisi klien.
- **Warna Gradasi Profil/Avatar Psikolog (Counseling Screen Avatar)**:
  - Memperbaiki warna gradasi lingkaran avatar inisial nama psikolog di `counseling_screen.dart` dan `psychologist_list_screen.dart`. Sebelumnya, gradasi warna menggunakan campuran oranye (primary) dan biru tua (`#0044BB`) yang berbenturan secara visual (komplementer) dan menghasilkan warna tengah keruh/kotor. Gradasi kini diganti secara dinamis berbasis model warna HSL dari `primary` institusi kustom (misal: oranye terang ke oranye gelap), menciptakan efek gradasi monokromatik/analogous yang elegan, estetik, dan selaras dengan identitas merek Super Admin.
- **Validasi ID Psikolog Saat Booking Konseling (Booking Submission)**:
  - Memperbaiki kegagalan pengiriman pesanan konseling (*stuck* dengan pesan kesalahan "psikolog_id wajib diisi"). Sebelumnya, aplikasi memaksa konversi ID psikolog (`psikolog_id`) dan slot (`slot_id`) menjadi integer (`int`). Padahal, di dalam database backend asli, ID psikolog menggunakan tipe data string (misal: `"P001"` atau UUID). Hal ini menyebabkan kegagalan konversi (hasil parsing berupa `0`) sehingga server menolak pemesanan. Solusinya, tipe parameter pada fungsi `createBooking` diubah menjadi `dynamic` dan rantai ekstraksi ID disesuaikan agar langsung mengirimkan format string mentah dari backend tanpa konversi paksa.
  - Menambahkan *helper* ekstraksi ID (`resolveSlotPsikologId` dan `resolveDetailPsikologId`) yang mendukung pencarian kunci multi-casing (case-insensitive) seperti `psikolog_id`, `PsikologID`, `dosen_id`, `DosenID`, `psychologist_id`, `psychologistId`, `PsychologistID`. Hal ini memisahkan deteksi ID psikolog agar tidak membaca kunci `id` / `ID` milik slot secara langsung (yang tadinya menyebabkan slot ID tertukar menjadi ID psikolog).
  - Memperbaiki alur pencarian psikolog di halaman beranda konseling (`counseling_screen.dart`) dan daftar psikolog (`psychologist_list_screen.dart`) dengan melakukan ekstraksi parameter `id`, `name`, `specialization`, dan status `isActive` secara case-insensitive agar link query parameter navigasi tidak pernah terkirim kosong.
- **Pembaruan Riwayat Konseling Real-Time (Real-time Booking Updates)**:
  - Mengimplementasikan sinkronisasi riwayat booking konseling secara *real-time* di dalam `counseling_screen.dart` agar status persetujuan sesi (seperti status `DIKONFIRMASI` atau `PENDING`) terupdate otomatis di layar tanpa perlu memicu muat ulang manual (pull-to-refresh). Hal ini dicapai dengan menjalankan *polling timer* periodik (setiap 3 detik) yang memicu pemanggilan `loadMyBookings(silent: true)` di latar belakang, merespons pembaruan status server dengan cepat dan mulus tanpa memicu kedipan efek pemuatan (*shimmer*).
- **Branding Warna Notifikasi In-App (Dynamic Notification SnackBar Colors)**:
  - Menyelaraskan warna latar belakang SnackBar notifikasi in-app (`NotificationService`) agar tidak lagi menggunakan warna biru tua hardcoded (`#1A3BAA`) untuk seluruh jenis pesan. Sekarang, warna latar belakang ditentukan secara dinamis menggunakan nilai dari Super Admin (lewat `ThemeProvider.current`): warna `success` untuk pesan keberhasilan, `colorError` untuk penolakan/pembatalan, `warning` untuk peringatan, dan `primary` untuk notifikasi info lainnya, menghasilkan UI yang konsisten dan selaras dengan tema institusi.



## [10 Juli 2026 - 18:25 WIB]

### Diperbaiki (Fixed)
- **Beasiswa (Pendaftaran Ulang & Katalog)**:
  - Memperbaiki filter katalog beasiswa agar tetap menampilkan beasiswa yang berstatus ditolak dengan tombol khusus berwarna merah bertuliskan "Ditolak".
  - Memperbaiki verifikasi beasiswa aktif di halaman `ApplyScholarshipScreen` dan tombol pendaftaran agar tidak memblokir mahasiswa ketika mengajukan beasiswa baru jika beasiswa sebelumnya sudah ditolak.
- **Kesehatan & Booking Klinik**:
  - Mengatasi masalah "looping terus" (loading spinner macet) saat membatalkan janji temu klinik dengan memperbaiki *context shadowing* pada context builder dialog.
  - Memperbaiki eror crash provider dengan mengganti `context.watch` menjadi `context.read` di dalam callback tombol batalkan janji temu.
  - Memperbaiki *context shadowing* pada dialog sukses pelaporan kesehatan di `report_health_screen.dart`.
  - Memperbaiki bug kritis di mana penyesuaian jadwal ulang (*reschedule*) memantul (sempat berubah namun kembali ke semula dalam beberapa detik). Bug ini diselesaikan dengan melengkapi eksekusi penulisan *cache* lokal ke `SharedPreferences` tepat sesaat sesudah pemanggilan API *reschedule* berhasil diproses (di dalam `rescheduleHealthBooking`), memastikan jadwal baru terkunci permanen di perangkat pengguna secara hibrida.
  - Memperbaiki eror crash provider dengan mengganti `context.watch` menjadi `context.read` di dalam form penjadwalan ulang (`health_booking_form_screen.dart`).
  - **BKU Assurance**: Memperbaiki label status klaim (seperti "Final Approved") yang tidak tersinkronisasi dan selalu menampilkan "Menunggu Verifikasi" dengan membuat logika perbandingan string status menjadi lebih fleksibel dan *case-insensitive* (`insurance_claim_screen.dart`).
## [10 Juli 2026 - 17:15 WIB]

### Diperbaiki (Fixed)
- **Otentikasi (Navigasi Error 401)**: Memperbaiki crash `Navigator.onGenerateRoute was null` saat menangani respons HTTP 401 (Unauthorized) di `ApiInterceptor` dengan menggunakan navigasi `GoRouter` global dari `apiNavigatorKey.currentContext` sebagai pengganti native `Navigator`.
- **Startup Race Condition (Token & API)**: Menghapus inisialisasi pemanggilan data API `loadAllData()` pada constructor `StudentProvider` untuk menghindari pengiriman request tanpa token saat startup (SplashScreen). Pemanggilan `loadAllData()` dipindahkan ke `initState` pada `DashboardScreen` untuk memastikan token sudah dimuat.
- **Tipe Data Runtime (Type Mismatch Klinik)**: Mengoreksi pemetaan tipe data di `StudentRepositoryImpl` pada metode `getHealthSchedules()`, `getHealthBookings()`, dan beberapa metode pemetaan list lainnya agar mengembalikan List yang dideklarasikan dengan tipe data kelas entitas dasar (bukan model turunan konkret) menggunakan `.map<Type>()`. Ini menyelesaikan eror runtime `type '() => HealthSchedule' is not a subtype of type '(() => HealthScheduleModel)?'` dan eror `type 'HealthBooking' is not a subtype of type 'HealthBookingModel'`.

## [10 Juli 2026 - 16:41 WIB]

### Ditambahkan (Added)
- **Komponen Global (Loading Dialog)**: Menambahkan `BkuLoadingDialog` sebagai komponen antarmuka standar (overlay layar penuh) untuk memberikan umpan balik visual bertuliskan "Mengirim data..." pada saat melakukan *submit* form data (Create, Edit, Delete).
- **Stabilitas Build (iOS Simulator)**: Menambahkan implementasi _mock_ lokal (`mobile_scanner_mock.dart`) untuk menonaktifkan sementara plugin `mobile_scanner` saat dijalankan di emulator iOS, mengatasi masalah gagal _compile_ akibat _dependency_ GoogleMLKit yang tidak didukung penuh pada arsitektur ARM64 Rosetta di mesin Apple Silicon.
- **String Extension**: Menambahkan metode `toSentenceCase()` dan `toTitleCase()` di `string_extensions.dart` untuk memastikan kapitalisasi status yang seragam.
- **Upload Klaim Asuransi**: Mengintegrasikan `image_picker` untuk memilih file dari kamera dan galeri saat mengajukan klaim asuransi (`insurance_claim_screen.dart`).
- **Upload Bukti Aspirasi**: Mengintegrasikan `image_picker` agar mahasiswa dapat mengunggah bukti aspirasi menggunakan Kamera, Galeri, atau File Explorer secara langsung (Dikerjakan oleh ANGGI).


### Changed
- **Status Text Capitalization**: Menerapkan Title Case pada teks status aplikasi di seluruh fitur (Beasiswa, Banding, Klinik Kesehatan, ds.) untuk memastikan konsistensi teks UI (contoh: `Menunggu`, `Disetujui`).
- **Loading Indicator & Error Handling**: Menerapkan `BkuLoadingDialog` secara global pada form pendaftaran, banding, booking klinik, dan hapus janji, dengan memunculkan pesan _Snackbar_ sukses atau modal _error_ berdasarkan kode HTTP menggunakan `ErrorHandler`.
- **Library Updates**: Menonaktifkan sementara (komentar) dependensi `mobile_scanner` pada `pubspec.yaml` untuk memperlancar proses kompilasi awal.

- **Beasiswa (Duplicate Check)**: Mengganti modal dialog pencegahan pendaftaran ganda (duplicate application) dengan antarmuka `showRejectionBottomSheet` modern yang dilengkapi dengan garis waktu (timeline) statis.
- Standardized `BkuLoadingDialog` overlay to replace raw `CircularProgressIndicator` across all features.
- Added `BkuLoadingDialog` to the student achievement (prestasi) feature for Create, Update, and Delete operations to prevent UI freeze and duplicate submissions.
- Added `BkuLoadingDialog` to the Kencana Banding submission to provide visual feedback during the fast backend API request.

- **Kencana Banding**: Menggunakan `BkuLoadingDialog` pada saat pengiriman pengajuan banding dan mengoptimasi transisi pemunculan `SnackBar` agar instan (tanpa *delay*).
- **Booking Klinik**: Memperbarui form *booking*, hapus janji, dan *reschedule* agar menggunakan `BkuLoadingDialog` dan menampilkan *error message* global dari `ErrorHandler`. Pembaruan UI otomatis (refresh) diaktifkan jika terdapat perubahan.
- **Klaim Asuransi**: Mengubah format nilai uang dengan `NumberFormat` ("Rp XXX.XXX") dan mempertegas desain _label text_ menjadi tebal dan hitam (bold/black) agar lebih terbaca.
- **Data Mahasiswa (`StudentProvider`)**: Memperkuat fungsi *parsing* JSON bersarang untuk data profil (terutama info akademik `ipk`, `semester`, dan `total_sks`) untuk mengakomodasi berbagai skema respons API.

### Diperbaiki (Fixed)
- Menyelesaikan seluruh masalah peringatan dan anjuran (_warnings_ & _lints_) menggunakan `dart fix --apply` dan diverifikasi dengan 0 isu pada `flutter analyze`.

## [10 Juli 2026 - 14:00 WIB]

### Ditambahkan (Added)
- **Komponen Global (Loading Dialog)**: Menambahkan `BkuLoadingDialog` sebagai komponen antarmuka standar (overlay layar penuh) untuk memberikan umpan balik visual bertuliskan "Mengirim data..." pada saat melakukan *submit* form data (Create, Edit, Delete).
- **Stabilitas Build (iOS Simulator)**: Menambahkan implementasi _mock_ lokal (`mobile_scanner_mock.dart`) untuk menonaktifkan sementara plugin `mobile_scanner` saat dijalankan di emulator iOS, mengatasi masalah gagal _compile_ akibat _dependency_ GoogleMLKit yang tidak didukung penuh pada arsitektur ARM64 Rosetta di mesin Apple Silicon.

### Changed
- Standardized `BkuLoadingDialog` overlay to replace raw `CircularProgressIndicator` across all features.
- Added `BkuLoadingDialog` to the student achievement (prestasi) feature for Create, Update, and Delete operations to prevent UI freeze and duplicate submissions.
- Added `BkuLoadingDialog` to the Kencana Banding submission to provide visual feedback during the fast backend API request.
- Fixed a bug in `report_health_screen.dart` where the `BkuLoadingDialog` was stuck looping forever because it was hidden *after* the success dialog was pushed onto the navigation stack.
- Removed duplicate success `SnackBar` ("pengajuan berhasil terkirim") in `kencana_banding_screen.dart` to prevent overlap with the global success notification dialog.
- Fixed scholarship application logic in `StudentProvider` to instantly persist the 'Applied' status locally, preventing users from opening the form for another scholarship if they have already applied.
- Updated `ApplyScholarshipScreen` to correctly display the rejection UI (`ubk_rejection.png`) when users attempt to register for multiple scholarships simultaneously.
- Replaced error `SnackBar` with large `CustomDialog` in `apply_scholarship_screen.dart` and `report_achievement_screen.dart` to ensure users see validation errors.
- Improved `StudentProvider` data mapping by implementing a recursive deep search (`_extractValue`) to find nested variables like `ipk`, `semester`, and `total_sks`, fixing the 0.00 IPK bug.
- Centralized all `DioException` and `Exception` handling globally through a new `ErrorHandler` class to provide human-readable (non-technical) error messages.

### Fixed
- Addressed silent failure bug during scholarship submission where `BkuLoadingDialog.hide` prematurely popped the root navigator.
- Addressed Laravel 422 validation parsing by recursively flattening the `errors` object in `ErrorHandler` to display specific missing fields directly to the user.
- Fixed all pending 12 `flutter analyze` warnings, including `WillPopScope` deprecation, `super.key` conversions, and unused imports.

- **Form UX (Loading Indicator & Success Dialog)**: Menyeragamkan UX seluruh fitur yang mengirimkan data dengan mengganti *loading* di tombol (`CircularProgressIndicator`) menjadi `BkuLoadingDialog` agar tampilan lebih bersih dan tombol tidak dapat diklik dua kali. Telah diterapkan pada halaman-halaman prioritas tinggi:
  - Form Pendaftaran Beasiswa
  - Form Pemesanan Konseling
  - Form Penyampaian Aspirasi Suara Mahasiswa
  - Form Pelaporan Kesehatan 
  - Form Pembuatan Transaksi Keuangan Ormawa (juga ditambahkan _CustomDialog_ sukses yang sebelumnya hilang)
  - Form Pengajuan Proposal Ormawa
- **Pendaftaran Beasiswa**: Memindahkan letak _checkbox_ persetujuan pernyataan ke bagian paling bawah (tepat di atas tombol kirim) agar lebih intuitif sesuai alur pengisian form.
- **Pendaftaran Beasiswa**: Menyesuaikan interaksi hitungan karakter minimum pada bagian "Alasan dan Motivasi" agar indikator peringatannya selalu muncul sejak awal memuat layar (menggunakan `AutovalidateMode.always`), tidak hanya muncul ketika validasi pengiriman gagal.

## [10 Juli 2026 - 10:15 WIB]

### Ditambahkan (Added)
- **Konseling Mahasiswa**: Menambahkan fitur lampiran (mendukung berkas Image dan PDF) saat melakukan _booking_ sesi konseling. Integrasi via `FilePicker` dan form-data terpusat pada `StudentCounselingProvider`.
- **Notifikasi Ormawa**: Menambahkan sistem navigasi pintar pada `ormawa_notifications_screen.dart` di mana menekan "Tutup & Lihat Detail" akan secara otomatis mengarahkan *user* ke layar (Proposal, Rekrutmen, Aspirasi, Pengumuman) berdasarkan `notification.type`.
- **Rekrutmen Ormawa**: Menambahkan fitur _Filter Sheet_ berdasarkan status (Semua, Menunggu, Diterima, Ditolak) pada daftar pelamar di `ormawa_recruitment_screen.dart`.
- **Booking Konseling (Psikolog)**: Menambahkan _Modal Bottom Sheet_ interaktif saat mengklik daftar permintaan masuk (Booking) yang menampilkan rincian profil mahasiswa, mode konseling, jadwal, serta keluhan spesifik. Tombol aksi (Konfirmasi/Tolak) dipindahkan ke dalam *sheet* ini agar tampilan daftar menjadi lebih rapi.

### Diubah (Changed)
- `[10:15 WIB]` **UI/UX (Skeleton Loading)**: Menggantikan tampilan indikator pemuatan statis melingkar penuh (`CircularProgressIndicator`) secara masif di seluruh modul layar utama (Dasbor Mahasiswa, Dasbor Ormawa, Dasbor Psikolog, Dasbor Tenaga Kesehatan, Rekrutmen, Riwayat, dll.) dengan antarmuka _Skeleton Loading_ beranimasi (`BkuShimmerList`, `BkuShimmer`), meningkatkan impresi visual kecepatan aplikasi (_perceived performance_) selagi pengguna menunggu data dari server.
- `[10:15 WIB]` **Konsistensi Teks & UI**: Menstandardisasikan seluruh warna teks untuk judul bagian (_section titles_) dan label deskripsi menu di _Service Grids_ yang sebelumnya tertahan mengikuti warna dinamis (contoh: Oranye) kembali ke warna teks hitam natural standar sistem (`AppColors.onSurface`).
- **Tema & Warna (Mobile Theme)**: Mengubah warna-warna *container* turunan (seperti `successContainer`, `warningContainer`) dan warna *tertiary* pada `MobileThemeColors` agar dihitung secara dinamis di Flutter menggunakan `Color.lerp` berdasarkan warna dasar dari API, secara resmi menghilangkan semua nilai statik (*hardcode*).
- **Tema & Warna (Mobile Theme Global Refactor)**: Melakukan refaktorisasi masif untuk mendukung skema warna dinamis (Remote Config) berdasarkan konfigurasi Super Admin via API `/api/public/theme`. Mengonversi lebih dari 1.050 referensi statik `AppColors` menjadi `Theme.of(context).colorScheme` dan `context.watch<ThemeProvider>()` pada lebih dari 110 _file_ UI. Properti `AppColors` diubah menjadi _mutable_ agar referensi statik (`AppTextStyles`) tetap dapat menyerap warna terbaru.
- **Navigasi Global**: Melakukan redesain besar pada `UnifiedBottomNavBar` menjadi gaya modern memanjang dan menempel penuh pada bagian bawah layar (*floating bottom* dihilangkan). Desain mempertahankan tema *gradient primary* khas corak batik, dilengkapi animasi *expanding pill* (gaya Salomon) di mana item yang aktif akan melebar untuk menampilkan teks dalam kapsul putih dinamis.
- **Dasbor Psikolog**: Merombak UI bagian "Layanan Utama" dari yang sebelumnya menggunakan _horizontal scroll_ (`ListView`) menjadi layout kotak _grid_ rapi (`GridView.count`), menyelaraskan konsistensi visual dengan dasbor mahasiswa.
- **Pencatatan Sesi (Psikolog)**: Meningkatkan kapabilitas penelusuran *error* dengan menangkap `DioException` pada proses "Simpan Catatan Asesmen" dan "Log Aktivitas Sesi" agar _payload_ kegagalan dari *backend* dapat dibaca lebih jelas saat _debugging_.
- **Pendaftaran Beasiswa**: Menghapus logo BKU ekstra di atas ilustrasi penolakan pada `apply_scholarship_screen.dart` agar tampilan UI terfokus pada gambar 3D yang kini sudah terintegrasi logo secara mendalam, serta memastikan ukuran *rendering* gambar lebih optimal.
- **Program Pemerintah**: Memperbaiki masalah *context shadowing* pada layar rincian beasiswa (`scholarship_screen.dart`) sehingga tombol "Ubah Data Pendaftaran" dan "Batalkan Pendaftaran" di dalam _Modal Bottom Sheet_ kembali responsif.
- **Prestasi Mahasiswa**: Menyesuaikan letak _Floating Action Button_ (FAB) pada `achievement_screen.dart` dengan menghilangkan properti `extendBody: true` agar komponen ini tidak memotong bagian bawah konten atau bertabrakan dengan navigasi sistem.
- **Prestasi Mahasiswa**: Mengubah metode _url launcher_ untuk membuka dokumen/PDF agar menggunakan `LaunchMode.inAppBrowserView` demi menghindari bug _looping_ tab pada browser bawaan perangkat.
- **Rekrutmen Ormawa**: Mengganti `RadioListTile` (yang mulai _deprecated_ di Flutter 3.32) dengan `ListTile` khusus untuk menghindari _warnings_ kompilasi, membuat tampilan opsi filter menjadi lebih bersih.

### Diperbaiki (Fixed)
- `[11:30 WIB]` **Stabilitas Build (Release APK)**: Menambahkan _permission_ `android.permission.ACCESS_NETWORK_STATE` pada `AndroidManifest.xml` utama untuk memperbaiki _bug_ di mana aplikasi versi APK (_release_) gagal melakukan _request_ API (mengembalikan _DioException bad response_) karena sistem Android memerlukan izin tambahan untuk mengecek status ketersediaan jaringan sebelum mengeksekusi _request_ HTTP.
- `[11:30 WIB]` **Codebase Linter & Auto-Fix**: Memperbaiki puluhan peringatan `Statements in an if should be enclosed in a block` pada modul Mahasiswa dan Ormawa, serta memperbaiki beberapa _syntax error_ (bentrok tanda kurung pada `SliverFillRemaining` dan penetapan nilai `final` pada fitur pencarian) yang sempat muncul pasca pergantian kode skema warna secara masif.
- **Profil (Sinkronisasi Data Web & Mobile)**: Memperbaiki *bug* arsitektur di mana gambar profil (serta Email Institusi) kerap tertinggal (menampilkan versi lama atau kosong) pada aplikasi Mobile setelah proses unggah (*upload*). Hal ini diakibatkan oleh *backend* yang hanya memperbarui tabel `users` dan mengabaikan tabel `mahasiswa`. Mengimplementasikan *patch* sinkronisasi ganda pintar pada `StudentRepositoryImpl.getProfile` yang otomatis mencaplok (menimpa) nilai `avatar` dan `email` langsung dari tabel akun utama (`/auth/me`), menjamin konsistensi data 100% sejajar antara aplikasi Mobile dan Website.
- **Tema & Warna (Hex Validator)**: Memperbaiki masalah pelewatan nilai ganda pada validator kode warna di `mobile_theme.dart` (seperti `##FF0000`) dengan memusatkan konversi pada `_hexToColor()` dan menambahkan implementasi sapu bersih `replaceAll('#', '')`. Ini menjamin *parsing* konfigurasi warna dari server selalu steril dari seluruh sisa simbol pagar, memblokir *crash* atau bentrok format *string* secara permanen.
- **Profil (Avatar Fallback & Cache-Busting)**: Memperkuat integrasi `Image.network` pada halaman Profil Mahasiswa dengan menambahkan metode *fallback* berbasis `ClipOval` yang tangguh dan warna latar statik `Colors.orange` untuk mendeteksi *corrupt file*. Diikuti dengan implementasi *cache-busting* (`?v=timestamp`) demi memastikan pembaruan avatar segera tampil secara otomatis seusai mengunggah gambar. Memastikan pengunggahan *multipart file* ke server diarahkan dengan presisi pada parameter _key_ `'foto'` untuk modul Mahasiswa agar diterima *backend* tanpa *error* "File tidak ditemukan".
- **Profil (UI & Tipografi)**: Menyesuaikan ulang hierarki visual teks pada _header_ setiap sesi menu utama (contoh: "Data Pribadi Mahasiswa") di `profile_screen.dart` dari `titleMd` menjadi `titleSm`. Perubahan ini menyusutkan ukuran huruf agar porsinya terlihat proporsional dan tidak memakan terlalu banyak ruang di layar.
- **Autentikasi (Spam Sesi Berakhir)**: Memperbaiki *bug race condition* pada `ApiInterceptor` yang menyebabkan notifikasi "Sesi telah berakhir" muncul berulang kali (*spam*) saat beberapa pemanggilan API (khususnya di Dasbor Psikolog) merespons status 401 secara bersamaan. Mengimplementasikan *debounce flag* dan merapikan alur pembersihan *state* di `AuthService.loadSession()` sehingga rute dikunci aman ke halaman `/login` tanpa menyebabkan *infinite loop*.
- **Bug Looping Autentikasi/Sesi Browser**: Menghilangkan _bug looping browser_ (terutama pada iOS) di layar pengaturan Tenaga Kesehatan dan Kencana Session yang sebelumnya macet saat mencoba meluncurkan URL persetujuan aplikasi.
- **Notifikasi (Psikolog)**: Memperbaiki masalah aplikasi yang berhenti merespons (_stuck flow_ / _force close_) saat menekan opsi 'CEK' pada _Snackbar_ "Pengingat Sesi Konseling". Pemanggilan `Navigator.pop` telah dihapus sehingga _Snackbar_ dapat langsung merutekan halaman tanpa kesalahan referensi _context_.
- **Upload Avatar (401 Unauthorized)**: Memperbaiki masalah gagal unggah (*upload*) foto profil yang menyebabkan aplikasi terhenti (*hang/blank screen*). Memastikan injeksi *header* `Authorization: Bearer <TOKEN>` secara manual dari `SharedPreferences` pada seluruh rute pengunggahan (`MultipartRequest`) di `student_repository_impl.dart`, `tk_repository_impl.dart`, dan `counseling_repository_impl.dart`.
- **Stabilitas UI (Async Gaps)**: Memperbaiki puluhan peringatan `use_build_context_synchronously` pada halaman pelaporan dan booking dengan menyuntikkan pengecekan `if (!mounted) return;` pasca-eksekusi *async* untuk mencegah UI melakukan *re-render* pada *state* yang sudah dibuang.
- **Codebase Linter & Auto-Fix**: Memperbaiki 694 konflik atribut `const` yang rusak akibat pergantian tipe konstanta statik menjadi dinamis (*theme config*), serta menghapus lebih dari 163 sintaks *import* usang menggunakan skrip pintar dan `dart fix`. Repositori saat ini 100% bebas dari _analyzer issues_ (`flutter analyze` melaporkan 0 masalah).
- **Tema & Warna (Gradient Sinkronisasi)**: Memperbaiki celah/bug pada UI di mana warna gradien dan warna _container_ sekunder di _App Bar_ masih tertahan dan bergradasi (belang) menggunakan warna biru bawaan sistem. Menambahkan logika _fallback_ cerdas pada `MobileThemeColors.fromJson` yang akan dengan cerdas membuang (mengabaikan) nilai dari Super Admin jika berisi sisa warna biru _default_, memaksa sistem untuk menciptakan turunan gradien 100% mulus dari warna _Primary_ (contoh: Oranye) yang digunakan saat ini.
- **Scroll Fisika (Bug Melar)**: Memperbaiki masalah visual "melar" pada daftar (_scrollable list_) yang mengganggu pengguna. Pertama, menerapkan `ClampingScrollPhysics` secara global untuk menghentikan pantulan berlebihan (_rubber-banding_) pada iOS. Kedua, mengimplementasikan `NoStretchScrollBehavior` pada `MaterialApp` untuk mematikan secara total efek distorsi/tarikan _Material 3 Stretch Overscroll_ bawaan dari Android 12+.
- **BkuAppBar (Ruang Kosong Raksasa)**: Memperbaiki *bug* pada `bku_app_bar.dart` di mana *Fixed App Bar* (`isExpandable: false`) secara keliru mewarisi tinggi 160px sehingga menyisakan ruang oranye raksasa yang tampak seperti "melar" (contoh: pada halaman Beasiswa). Telah dikunci aman pada ukuran presisi `kToolbarHeight`.
- **BkuAppBar (Background Overflow di SliverAppBar)**: Memperbaiki *bug* visual "kebesaran" (ruang oranye raksasa ke bawah) pada *Expandable App Bar* (`Jalur 2`) saat di-*scroll*. *Bug* ini disebabkan oleh `Align` dan `Container` statis dengan tinggi `expandedHeight` yang menimpa kalkulasi `flexibleSpace`, sehingga *background* tidak mau menyusut saat di-*scroll*. `LayoutBuilder` kini telah dipindah ke hirarki terluar untuk merespons *constraints* secara akurat.
- **BkuAppBar (Ukuran Proporsional)**: Menyesuaikan parameter `expandedHeight` dari 160px menjadi 130px di seluruh halaman (seperti Konseling, Beasiswa, Organisasi, dll.) agar *header* tidak terlihat terlalu besar dan memakan porsi layar secara berlebihan, memberikan tampilan yang lebih seimbang (*compact*).
- **Tema & Warna (Gradient Sinkronisasi pada Kartu)**: Memperbaiki celah/bug pada UI di mana warna gradien pada kartu ucapan selamat datang di `health_screen.dart` dan beberapa _container_ sekunder di `report_health_screen.dart` masih tertahan menggunakan *hardcoded hex color* biru `Color(0xFF1E40AF)`. Kini kartu-kartu tersebut telah sepenuhnya tersinkronisasi menggunakan `ThemeProvider.primaryGradient` yang sama.
- **Gambar Profil di Dashboard**: Memperbaiki masalah pada halaman utama (_Student Dashboard_) di mana gambar profil di sudut kiri atas (_App Bar_) tertahan hanya menampilkan ikon pengguna standar secara *hardcoded*, padahal profil pengguna sudah memiliki foto aslinya. Foto kini memuat dari server menggunakan URL yang tervalidasi dengan aman melalui `ApiGate.getImageUrl()`.
- **Crashes Gambar Profil (NetworkImage)**: Mencegah kegagalan fatal pada aplikasi (*crash red screen*) saat melakukan _Hot Restart_ akibat `NetworkImage` mencoba merender gambar dengan *path* lokal (`/uploads/...`) tanpa *host* yang sah. Seluruh path URL avatar pada modul Profil, Konseling, Ormawa, dan Tenaga Kesehatan kini telah dibungkus dengan aman menggunakan filter validasi `ApiGate.getImageUrl()`.
- **Bug Gradien Konseling**: Memperbaiki masalah *type error* kompilasi pada `counseling_screen.dart` (`The element type 'List<Color>' can't be assigned to the list type 'Color'`) akibat pembungkusan warna gradien berantai secara tidak sengaja di `ThemeProvider`.